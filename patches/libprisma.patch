diff --git a/libprisma/SyntaxHighlighter.cpp b/libprisma/SyntaxHighlighter.cpp
index b79fde1..39f4d11 100644
--- a/libprisma/SyntaxHighlighter.cpp
+++ b/libprisma/SyntaxHighlighter.cpp
@@ -180,10 +180,12 @@ void SyntaxHighlighter::matchGrammar(std::string_view text, TokenList& tokenList
                 {
                     // at least one Token object was removed, so we have to do some rematching
                     // this can only happen if the current pattern is greedy
+                    // XP walk: designated init -> positional (C7555, v141_xp C++17).
+                    // RematchOptions layout: token, reach, j.
                     RematchOptions nestedRematch = {
-                        .token = token.name(),
-                        .reach = reach,
-                        .j = x
+                        token.name(), // token
+                        reach, // reach
+                        x // j
                     };
 
                     matchGrammar(text, tokenList, grammar, currentNode->prev, pos, &nestedRematch);
