diff --git a/common/common.gypi b/common/common.gypi
index f706ff1..e978d44 100644
--- a/common/common.gypi
+++ b/common/common.gypi
@@ -40,7 +40,6 @@
         'build_win%': '<(build_win)',
         'build_mac%': '<(build_mac)',
         'build_linux%': '<(build_linux)',
-        'special_build_target%': '',
 
         'conditions': [[ '"<(special_build_target)" == "osx"', {
           'build_macold': 0,
@@ -61,7 +60,7 @@
       'conditions': [[ 'build_mac and not build_osx', {
         'libs_loc%': '<(DEPTH)/../../../Libraries/macos',
       }, {
-        'libs_loc%': '<(DEPTH)/../../../Libraries',
+        'libs_loc%': '<(DEPTH)/../../../Libraries-walk',
       }]],
 
       'build_os%': '<(build_os)',
@@ -73,8 +72,7 @@
       'build_osx%': '<(build_osx)',
       'build_linux%': '<(build_linux)',
 
-      'special_build_target%': '<(special_build_target)',
-      'ci_build%': '',
+      'special_build_target%': '',
       'build_standard_win%': 'c++17',
       'submodules_loc%': '<(DEPTH)/..',
       'private_loc%': '<(DEPTH)/../../../DesktopPrivate',
@@ -85,7 +83,6 @@
     'build_mac%': '<(build_mac)',
     'build_linux%': '<(build_linux)',
     'special_build_target%': '<(special_build_target)',
-    'ci_build%': '<(ci_build)',
     'build_standard_win%': '<(build_standard_win)',
     'libs_loc%': '<(libs_loc)',
     'submodules_loc%': '<(submodules_loc)',
diff --git a/common/win.gypi b/common/win.gypi
index 60647a2..d5036bc 100644
--- a/common/win.gypi
+++ b/common/win.gypi
@@ -16,6 +16,13 @@
         'ZLIB_WINAPI',
         '_SCL_SECURE_NO_WARNINGS',
         '_USING_V110_SDK71_',
+        '_WIN32_WINNT=0x0501',
+        'WINVER=0x0501',
+        'NTDDI_VERSION=0x05010300',
+        # Disable breakpad crash reporting across ALL targets (lib_base's
+        # crash_report_writer.cpp lives in a separate target, so a Telegram-only
+        # define would leave USE_BREAKPAD on there). breakpad is not built for XP.
+        'TDESKTOP_DISABLE_CRASH_REPORTS',
       ],
       'msbuild_toolset': 'v142',
       'msvs_cygwin_shell': 0,
@@ -23,18 +30,19 @@
         'VCCLCompilerTool': {
           'ProgramDataBaseFileName': '$(OutDir)\\$(ProjectName).pdb',
           'DebugInformationFormat': '3',          # Program Database (/Zi)
-          'WarnAsError': 'true',
+          'WarnAsError': 'false',
           'AdditionalOptions': [
             '/std:<(build_standard_win)',
-            '/permissive-',
             '/Qspectre',
             '/MP',     # Enable multi process build.
+            '/bigobj', # apiwrap.cpp etc. exceed the COFF section limit without /GL (C1128).
             '/EHsc',   # Catch C++ exceptions only, extern C functions never throw a C++ exception.
             '/w14834', # [[nodiscard]]
             '/w15038', # wrong initialization order
             '/w14265', # class has virtual functions, but destructor is not virtual
-            '/experimental:preprocessor', # need for range-v3 see https://github.com/ericniebler/range-v3#supported-compilers
-            '/wd5105', # needed for `/experimental:preprocessor`, suppressing C5105 "macro expansion producing 'defined' has undefined behavior"
+            '/Zc:preprocessor', # range-v3 0.9.1 needs the conformant preprocessor (maintained
+                                # /Zc form vs deprecated /experimental); fixes variadic-pack expansion
+            '/wd5105', # suppress C5105 from the conformant preprocessor in Windows SDK headers
           ],
           'TreatWChar_tAsBuiltInType': 'false',
         },
@@ -95,12 +103,15 @@
               'FavorSizeOrSpeed': '1',             # Favor fast code (/Ot)
               'RuntimeLibrary': '0',               # Multi-threaded (/MT)
               'EnableEnhancedInstructionSet': '2', # Streaming SIMD Extensions 2 (/arch:SSE2)
-              'WholeProgramOptimization': 'true',  # /GL
+              'WholeProgramOptimization': 'true',  # /GL — REQUIRED: the no-/GL binary crashes
+                                                   # before log init on XP (codegen uses FP/CRT
+                                                   # helpers absent from fpcompat.lib). Slow LTCG
+                                                   # link is the price; do not disable.
             },
             'VCLinkerTool': {
               'GenerateDebugInformation': 'true',  # /DEBUG
               'OptimizeReferences': '2',
-              'LinkTimeCodeGeneration': '1',       # /LTCG
+              'LinkTimeCodeGeneration': '1',       # /LTCG (paired with /GL above)
             },
             'VCLibrarianTool': {
               'LinkTimeCodeGeneration': 'true',    # /LTCG
@@ -112,15 +123,6 @@
         [ '"<(special_build_target)" != "" and "<(special_build_target)" != "win" and "<(special_build_target)" != "uwp"', {
           'sources': [ '__Wrong_Special_Build_Target__' ],
         }],
-        [ '"<(ci_build)" == "1"', {
-          'msvs_settings': {
-            'VCLinkerTool': {
-              'AdditionalOptions': [
-                '/ignore:4099', # Ignore missing PDBs in CI builds.
-              ],
-            },
-          },
-        }]
       ],
     }],
   ],
diff --git a/modules/openssl.gypi b/modules/openssl.gypi
index 91f1a99..a1d295c 100644
--- a/modules/openssl.gypi
+++ b/modules/openssl.gypi
@@ -7,9 +7,12 @@
 {
   'conditions': [
     [ 'build_win', {
+      # XP walk: our Libraries-walk openssl is 1.0.2 (inc32/out32, libeay32/ssleay32),
+      # not the 1.1.1 layout upstream expects. openssl_help.h uses only classic APIs
+      # (SHA256_*, BN_*, RAND_bytes) that 1.0.2 provides.
       'libraries': [
-        '-llibcrypto',
-        '-llibssl',
+        '-llibeay32',
+        '-lssleay32',
         '-lWs2_32',
         '-lGdi32',
         '-lAdvapi32',
@@ -19,18 +22,18 @@
       'configurations': {
         'Debug': {
           'include_dirs': [
-            '<(libs_loc)/openssl_1_1_1/include',
+            '<(libs_loc)/openssl/inc32',
           ],
           'library_dirs': [
-            '<(libs_loc)/openssl_1_1_1/out32.dbg',
+            '<(libs_loc)/openssl/out32',
           ],
         },
         'Release': {
           'include_dirs': [
-            '<(libs_loc)/openssl_1_1_1/include',
+            '<(libs_loc)/openssl/inc32',
           ],
           'library_dirs': [
-            '<(libs_loc)/openssl_1_1_1/out32',
+            '<(libs_loc)/openssl/out32',
           ],
         },
       },
diff --git a/modules/qt.gypi b/modules/qt.gypi
index ee531d9..9c58abb 100644
--- a/modules/qt.gypi
+++ b/modules/qt.gypi
@@ -14,7 +14,7 @@
               [ 'build_osx', {
                 'qt_version%': '5.6.2',
               }, {
-                'qt_version%': '5.12.5',
+                'qt_version%': '5.15.16',
               }]
             ],
           },
@@ -51,6 +51,7 @@
               'qwindows',
               'qtfreetype',
               'qtpcre2',
+              'qtlibjpeg', # backs the qjpeg image-format plugin (Qt's bundled libjpeg).
             ],
           }],
           [ 'build_mac', {
@@ -131,7 +132,7 @@
     'qt_version%': '<(qt_version)',
     'conditions': [
       [ 'build_win', {
-        'qt_loc': '<(libs_loc)/Qt-<(qt_version)',
+        'qt_loc': 'C:/TBuild/xp-port/qt-xp-static-prefix',
       }, {
         'qt_loc': '<(qt_loc_unix)',
       }],
@@ -242,7 +243,6 @@
         '-lnimfplatforminputcontextplugin',
         '<@(qt_libs_release)',
         '<(linux_path_xkbcommon)/lib/libxkbcommon.a',
-        '<(linux_path_xkbcommon)/lib/libxkbcommon-x11.a',
         '<(PRODUCT_DIR)/obj.target/helpers/platform/linux/liblinux_glibc_wraps.a',
         #'<(linux_lib_ssl)', # added in lib_ton
         #'<(linux_lib_crypto)', # added in lib_ton
