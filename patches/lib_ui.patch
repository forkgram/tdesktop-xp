diff --git a/emoji_suggestions/emoji_suggestions.h b/emoji_suggestions/emoji_suggestions.h
index 1725a2a5..96b1ce6e 100644
--- a/emoji_suggestions/emoji_suggestions.h
+++ b/emoji_suggestions/emoji_suggestions.h
@@ -8,6 +8,13 @@
 
 #include <vector>
 
+// XP walk: Windows <rpcndr.h> (pulled via windows.h) defines `small` as a macro
+// (`#define small char`), which mangles the `using small = ...` alias below into
+// `using char = ...` (C2059). Drop the macro before declaring the type alias.
+#ifdef small
+#undef small
+#endif
+
 namespace Ui {
 namespace Emoji {
 
diff --git a/ui/effects/frame_generator.cpp b/ui/effects/frame_generator.cpp
index a50b4432..5e86fda6 100644
--- a/ui/effects/frame_generator.cpp
+++ b/ui/effects/frame_generator.cpp
@@ -39,7 +39,7 @@ FrameGenerator::Frame ImageFrameGenerator::renderCurrent(
 		Qt::AspectRatioMode mode) {
 	if (_image.isNull() && !_bytes.isEmpty()) {
 		_image = Images::Read({
-			.content = _bytes,
+			{}, _bytes,
 		}).image;
 		_bytes = QByteArray();
 	}
@@ -52,7 +52,7 @@ FrameGenerator::Frame ImageFrameGenerator::renderCurrent(
 		Qt::SmoothTransformation
 	).convertToFormat(QImage::Format_ARGB32_Premultiplied);
 	if (scaled.size() == size) {
-		return { .image = std::move(scaled) };
+		return { {}, std::move(scaled) };
 	}
 	auto result = QImage(size, QImage::Format_ARGB32_Premultiplied);
 	result.fill(Qt::transparent);
@@ -70,7 +70,7 @@ FrameGenerator::Frame ImageFrameGenerator::renderCurrent(
 		dst += dstPerLine;
 	}
 
-	return { .image = std::move(result), .last = true };
+	return { {}, std::move(result), true };
 }
 
 void ImageFrameGenerator::jumpToStart() {
diff --git a/ui/effects/panel_animation.cpp b/ui/effects/panel_animation.cpp
index 51144b50..c1677338 100644
--- a/ui/effects/panel_animation.cpp
+++ b/ui/effects/panel_animation.cpp
@@ -381,12 +381,12 @@ auto PanelAnimation::computeState(float64 dt, float64 opacity) const
 		: _finalInnerHeight;
 	const auto ratio = style::DevicePixelRatio();
 	return {
-		.opacity = opacity,
-		.widthProgress = widthProgress,
-		.heightProgress = heightProgress,
-		.fade = transition(1., dt),
-		.width = frameWidth / ratio,
-		.height = frameHeight / ratio,
+		opacity,
+		widthProgress,
+		heightProgress,
+		transition(1., dt),
+		frameWidth / ratio,
+		frameHeight / ratio,
 	};
 }
 
diff --git a/ui/effects/spoiler_mess.cpp b/ui/effects/spoiler_mess.cpp
index d4413de5..d53c64b8 100644
--- a/ui/effects/spoiler_mess.cpp
+++ b/ui/effects/spoiler_mess.cpp
@@ -37,18 +37,18 @@ constexpr auto kAutoPauseTimeout = crl::time(1000);
 	const auto ratio = style::DevicePixelRatio();
 	const auto size = style::ConvertScale(128) * ratio;
 	return {
-		.particleFadeInDuration = crl::time(200),
-		.particleShownDuration = crl::time(200),
-		.particleFadeOutDuration = crl::time(200),
-		.particleSizeMin = style::ConvertScaleExact(1.5) * ratio,
-		.particleSizeMax = style::ConvertScaleExact(2.) * ratio,
-		.particleSpeedMin = style::ConvertScaleExact(4.),
-		.particleSpeedMax = style::ConvertScaleExact(8.),
-		.particleSpritesCount = 5,
-		.particlesCount = 9000,
-		.canvasSize = size,
-		.framesCount = kDefaultFramesCount,
-		.frameDuration = kDefaultFrameDuration,
+		crl::time(200),
+		crl::time(200),
+		crl::time(200),
+		style::ConvertScaleExact(1.5) * ratio,
+		style::ConvertScaleExact(2.) * ratio,
+		style::ConvertScaleExact(4.),
+		style::ConvertScaleExact(8.),
+		5,
+		9000,
+		size,
+		kDefaultFramesCount,
+		kDefaultFrameDuration,
 	};
 }
 
@@ -56,18 +56,18 @@ constexpr auto kAutoPauseTimeout = crl::time(1000);
 	const auto ratio = style::DevicePixelRatio();
 	const auto size = style::ConvertScale(128) * ratio;
 	return {
-		.particleFadeInDuration = crl::time(300),
-		.particleShownDuration = crl::time(0),
-		.particleFadeOutDuration = crl::time(300),
-		.particleSizeMin = style::ConvertScaleExact(1.5) * ratio,
-		.particleSizeMax = style::ConvertScaleExact(2.) * ratio,
-		.particleSpeedMin = style::ConvertScaleExact(10.),
-		.particleSpeedMax = style::ConvertScaleExact(20.),
-		.particleSpritesCount = 5,
-		.particlesCount = 3000,
-		.canvasSize = size,
-		.framesCount = kDefaultFramesCount,
-		.frameDuration = kDefaultFrameDuration,
+		crl::time(300),
+		crl::time(0),
+		crl::time(300),
+		style::ConvertScaleExact(1.5) * ratio,
+		style::ConvertScaleExact(2.) * ratio,
+		style::ConvertScaleExact(10.),
+		style::ConvertScaleExact(20.),
+		5,
+		3000,
+		size,
+		kDefaultFramesCount,
+		kDefaultFrameDuration,
 	};
 }
 
@@ -151,13 +151,13 @@ struct Particle {
 		base::BufferedRandom<uint32> &random) {
 	const auto speed = RandomSpeed(descriptor, random);
 	return {
-		.start = (index * descriptor.framesCount * descriptor.frameDuration
+		(index * descriptor.framesCount * descriptor.frameDuration
 			/ descriptor.particlesCount),
-		.spriteIndex = RandomIndex(descriptor.particleSpritesCount, random),
-		.x = RandomIndex(descriptor.canvasSize, random),
-		.y = RandomIndex(descriptor.canvasSize, random),
-		.dx = speed.first,
-		.dy = speed.second,
+		RandomIndex(descriptor.particleSpritesCount, random),
+		RandomIndex(descriptor.canvasSize, random),
+		RandomIndex(descriptor.canvasSize, random),
+		speed.first,
+		speed.second,
 	};
 }
 
@@ -260,9 +260,9 @@ void PrepareDefaultSpoiler(
 	crl::async([=, &spoiler] {
 		const auto descriptor = descriptorFactory();
 		auto cached = ReadDefaultMask(name, SpoilerMessCached::Validator{
-			.frameDuration = descriptor.frameDuration,
-			.framesCount = descriptor.framesCount,
-			.canvasSize = descriptor.canvasSize,
+			descriptor.frameDuration,
+			descriptor.framesCount,
+			descriptor.canvasSize,
 		});
 		spoiler.cached = postprocess(cached
 			? std::make_unique<SpoilerMessCached>(std::move(*cached))
@@ -653,8 +653,8 @@ SpoilerMessFrame SpoilerMessCached::frame(int index) const {
 	const auto row = index / kFramesPerRow;
 	const auto column = index - row * kFramesPerRow;
 	return {
-		.image = &_image,
-		.source = QRect(
+		&_image,
+		QRect(
 			column * _canvasSize,
 			row * _canvasSize,
 			_canvasSize,
@@ -684,10 +684,12 @@ QByteArray SpoilerMessCached::serialize() const {
 	const auto skip = sizeof(Header);
 	auto result = QByteArray(skip, Qt::Uninitialized);
 	auto header = Header{
-		.version = kVersion,
-		.framesCount = _framesCount,
-		.canvasSize = _canvasSize,
-		.frameDuration = int32(_frameDuration),
+		kVersion,
+		{},
+		{},
+		_framesCount,
+		_canvasSize,
+		int32(_frameDuration),
 	};
 	const auto width = int(_image.width());
 	const auto height = int(_image.height());
diff --git a/ui/effects/spoiler_mess.h b/ui/effects/spoiler_mess.h
index 759f3fe4..4d1f0799 100644
--- a/ui/effects/spoiler_mess.h
+++ b/ui/effects/spoiler_mess.h
@@ -101,8 +101,8 @@ private:
 	const Fn<void()> _repaint;
 	crl::time _accumulated = 0;
 	crl::time _last = 0;
-	bool _animating : 1 = false;
-	bool _scheduled : 1 = false;
+	bool _animating = false;
+	bool _scheduled = false;
 
 };
 
diff --git a/ui/gl/gl_detection.cpp b/ui/gl/gl_detection.cpp
index e927ff65..60d29267 100644
--- a/ui/gl/gl_detection.cpp
+++ b/ui/gl/gl_detection.cpp
@@ -9,6 +9,7 @@
 #include "ui/gl/gl_shader.h"
 #include "ui/integration.h"
 #include "base/debug_log.h"
+#include "base/options.h"
 #include "base/platform/base_platform_info.h"
 
 #include <QtCore/QSet>
@@ -18,11 +19,14 @@
 #include <QtGui/QOpenGLFunctions>
 #include <QOpenGLWidget>
 
-#ifdef DESKTOP_APP_USE_ANGLE
+#ifdef Q_OS_WIN
 #include <QtGui/QGuiApplication>
 #include <qpa/qplatformnativeinterface.h>
-#include <EGL/egl.h>
-#endif // DESKTOP_APP_USE_ANGLE
+
+// XP walk: <EGL/egl.h> is the ANGLE/EGL header; the static XP Qt uses DESKTOP GL
+// (opengl32) and ships no EGL headers/lib. EGLExtensions() below is stubbed to
+// return empty (the desktop-GL platform has no "egldisplay"), so EGL is not needed.
+#endif // Q_OS_WIN
 
 #define LOG_ONCE(x) [[maybe_unused]] static auto logged = [&] { LOG(x); return true; }();
 
@@ -32,22 +36,20 @@ namespace {
 bool ForceDisabled/* = false*/;
 bool LastCheckCrashed/* = false*/;
 
-#ifdef DESKTOP_APP_USE_ANGLE
+#ifdef Q_OS_WIN
 ANGLE ResolvedANGLE/* = ANGLE::Auto*/;
+#endif // Q_OS_WIN
 
-QList<QByteArray> EGLExtensions(not_null<QOpenGLContext*> context) {
-	const auto native = QGuiApplication::platformNativeInterface();
-	Assert(native != nullptr);
-
-	const auto display = static_cast<EGLDisplay>(
-		native->nativeResourceForContext(
-			QByteArrayLiteral("egldisplay"),
-			context));
-	return display
-		? QByteArray(eglQueryString(display, EGL_EXTENSIONS)).split(' ')
-		: QList<QByteArray>();
-}
-#endif // DESKTOP_APP_USE_ANGLE
+base::options::toggle AllowX11NvidiaOpenGL({
+	// XP walk: designated -> positional (C7555). base::options::descriptor:
+	// id, name, description, defaultValue, scope, restartRequired.
+	kOptionAllowX11NvidiaOpenGL, // id
+	"Allow OpenGL on the NVIDIA drivers (X11)", // name
+	"Qt+OpenGL have problems on X11 with NVIDIA drivers.", // description
+	{}, // defaultValue
+	Platform::IsX11, // scope
+	true, // restartRequired
+});
 
 void CrashCheckStart() {
 	auto f = QFile(Integration::Instance().openglCheckFilePath());
@@ -59,6 +61,8 @@ void CrashCheckStart() {
 
 } // namespace
 
+const char kOptionAllowX11NvidiaOpenGL[] = "allow-linux-nvidia-opengl";
+
 Capabilities CheckCapabilities(QWidget *widget) {
 	if (!Platform::IsMac()) {
 		if (ForceDisabled) {
@@ -79,29 +83,36 @@ Capabilities CheckCapabilities(QWidget *widget) {
 		return true;
 	}();
 
-	CrashCheckStart();
-	const auto guard = gsl::finally([=] {
-		CrashCheckFinish();
-	});
+	auto format = QSurfaceFormat();
+	if (widget) {
+		if (!widget->window()->windowHandle()) {
+			widget->window()->createWinId();
+		}
+		if (!widget->window()->windowHandle()) {
+			LOG(("OpenGL: Could not create window for widget."));
+			return {};
+		}
+		format = widget->window()->windowHandle()->format();
+		format.setAlphaBufferSize(8);
+		widget->window()->windowHandle()->setFormat(format);
+	} else {
+		format.setAlphaBufferSize(8);
+	}
 
+	CrashCheckStart();
 	auto tester = QOpenGLWidget(widget);
+	tester.setFormat(format);
+	tester.grabFramebuffer(); // Force initialize().
 	if (!tester.window()->windowHandle()) {
 		tester.window()->createWinId();
 	}
-	if (!tester.window()->windowHandle()) {
-		LOG(("OpenGL: Could not create window for widget."));
-		return {};
-	}
-	auto format = tester.window()->windowHandle()->format();
-	format.setAlphaBufferSize(8);
-	tester.window()->windowHandle()->setFormat(format);
-	tester.setFormat(format);
-	tester.grabFramebuffer(); // Force initialize().
+	CrashCheckFinish();
 
 	const auto context = tester.context();
 	if (!context
-		|| !context->isValid()
-		|| !context->makeCurrent(tester.window()->windowHandle())) {
+		|| !context->isValid()/*
+		// This check doesn't work for a widget with WA_NativeWindow.
+		|| !context->makeCurrent(tester.window()->windowHandle())*/) {
 		LOG_ONCE(("OpenGL: Could not create widget in a window."));
 		return {};
 	}
@@ -168,13 +179,25 @@ Capabilities CheckCapabilities(QWidget *widget) {
 		}
 		LOG(("OpenGL Extensions: %1").arg(list.join(", ")));
 
-#ifdef DESKTOP_APP_USE_ANGLE
+#ifdef Q_OS_WIN
 		auto egllist = QStringList();
 		for (const auto &extension : EGLExtensions(context)) {
 			egllist.append(QString::fromLatin1(extension));
 		}
 		LOG(("EGL Extensions: %1").arg(egllist.join(", ")));
-#endif // DESKTOP_APP_USE_ANGLE
+#endif // Q_OS_WIN
+
+		if (::Platform::IsX11()
+			&& version
+			&& QByteArray(version).contains("NVIDIA")) {
+			// https://github.com/telegramdesktop/tdesktop/issues/16830
+			if (AllowX11NvidiaOpenGL.value()) {
+				LOG_ONCE(("OpenGL: Allow on NVIDIA driver (experimental)."));
+			} else {
+				LOG_ONCE(("OpenGL: Disable on NVIDIA driver on X11."));
+				return false;
+			}
+		}
 
 		return true;
 	}();
@@ -185,7 +208,7 @@ Capabilities CheckCapabilities(QWidget *widget) {
 	const auto version = u"%1.%2"_q
 		.arg(supported.majorVersion())
 		.arg(supported.majorVersion());
-	auto result = Capabilities{ .supported = true };
+	auto result = Capabilities{ true };
 	if (supported.alphaBufferSize() >= 8) {
 		result.transparency = true;
 		LOG_ONCE(("OpenGL: QOpenGLContext created, version: %1."
@@ -229,7 +252,8 @@ void ForceDisable(bool disable) {
 	}
 }
 
-#ifdef DESKTOP_APP_USE_ANGLE
+#ifdef Q_OS_WIN
+
 void ConfigureANGLE() {
 	qunsetenv("DESKTOP_APP_QT_ANGLE_PLATFORM");
 	const auto path = Ui::Integration::Instance().angleBackendFilePath();
@@ -280,6 +304,13 @@ void ChangeANGLE(ANGLE backend) {
 ANGLE CurrentANGLE() {
 	return ResolvedANGLE;
 }
-#endif // DESKTOP_APP_USE_ANGLE
+
+QList<QByteArray> EGLExtensions(not_null<QOpenGLContext*> context) {
+	// XP walk: desktop GL has no EGL display and the build links no libEGL, so
+	// report no EGL extensions.
+	return QList<QByteArray>();
+}
+
+#endif // Q_OS_WIN
 
 } // namespace Ui::GL
diff --git a/ui/gl/gl_detection.h b/ui/gl/gl_detection.h
index 633fba30..070d3fdc 100644
--- a/ui/gl/gl_detection.h
+++ b/ui/gl/gl_detection.h
@@ -8,15 +8,12 @@
 
 #include "base/flags.h"
 
-// ANGLE is used only on Windows with Qt < 6.
-#if defined Q_OS_WIN && QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
-#define DESKTOP_APP_USE_ANGLE
-#endif // Q_OS_WIN && Qt < 6
-
 class QOpenGLContext;
 
 namespace Ui::GL {
 
+extern const char kOptionAllowX11NvidiaOpenGL[];
+
 enum class Backend {
 	Raster,
 	OpenGL,
@@ -36,7 +33,7 @@ void DetectLastCheckCrash();
 [[nodiscard]] bool LastCrashCheckFailed();
 void CrashCheckFinish();
 
-#ifdef DESKTOP_APP_USE_ANGLE
+// Windows only.
 enum class ANGLE {
 	Auto,
 	D3D9,
@@ -48,6 +45,8 @@ enum class ANGLE {
 void ConfigureANGLE(); // Requires Ui::Integration being set.
 void ChangeANGLE(ANGLE backend);
 [[nodiscard]] ANGLE CurrentANGLE();
-#endif // DESKTOP_APP_USE_ANGLE
+
+[[nodiscard]] QList<QByteArray> EGLExtensions(
+	not_null<QOpenGLContext*> context);
 
 } // namespace Ui::GL
diff --git a/ui/gl/gl_image.cpp b/ui/gl/gl_image.cpp
index cb04fdd8..fd41213c 100644
--- a/ui/gl/gl_image.cpp
+++ b/ui/gl/gl_image.cpp
@@ -137,8 +137,8 @@ TexturedRect Image::texturedRect(
 		: clip.intersected(geometry);
 	if (visible.isEmpty()) {
 		return TexturedRect{
-			.geometry = Rect(visible),
-			.texture = Rect(0., 0., 0., 0.),
+			Rect(visible),
+			Rect(0., 0., 0., 0.),
 		};
 	}
 	const auto xFactor = texture.width() / geometry.width();
@@ -153,8 +153,8 @@ TexturedRect Image::texturedRect(
 		? _subimage
 		: _textureSize);
 	return {
-		.geometry = Rect(visible),
-		.texture = Rect(QRectF(
+		Rect(visible),
+		Rect(QRectF(
 			usedTexture.x() / dimensions.width(),
 			usedTexture.y() / dimensions.height(),
 			usedTexture.width() / dimensions.width(),
diff --git a/ui/gl/gl_shader.cpp b/ui/gl/gl_shader.cpp
index f306bbe6..872a40eb 100644
--- a/ui/gl/gl_shader.cpp
+++ b/ui/gl/gl_shader.cpp
@@ -57,21 +57,21 @@ void main() {
 ShaderPart VertexPassTextureCoord(char prefix) {
 	const auto name = prefix + QString("_texcoord");
 	return {
-		.header = R"(
+		R"(
 attribute vec2 )" + name + R"(In;
 varying vec2 )" + name + ";\n",
-		.body = R"(
+		R"(
 	)" + name + " = " + name + "In;\n",
 	};
 }
 
 ShaderPart FragmentSampleARGB32Texture() {
 	return {
-		.header = R"(
+		R"(
 varying vec2 v_texcoord;
 uniform sampler2D s_texture;
 )",
-		.body = R"(
+		R"(
 	result = texture2D(s_texture, v_texcoord);
 )" + (kSwizzleRedBlue
 	? R"(
@@ -92,13 +92,13 @@ QString FragmentYUV2RGB() {
 
 ShaderPart FragmentSampleYUV420Texture() {
 	return {
-		.header = R"(
+		R"(
 varying vec2 v_texcoord;
 uniform sampler2D y_texture;
 uniform sampler2D u_texture;
 uniform sampler2D v_texture;
 )",
-		.body = R"(
+		R"(
 	float y = texture2D(y_texture, v_texcoord).a - 0.0625;
 	float u = texture2D(u_texture, v_texcoord).a - 0.5;
 	float v = texture2D(v_texture, v_texcoord).a - 0.5;
@@ -108,12 +108,12 @@ uniform sampler2D v_texture;
 
 ShaderPart FragmentSampleNV12Texture() {
 	return {
-		.header = R"(
+		R"(
 varying vec2 v_texcoord;
 uniform sampler2D y_texture;
 uniform sampler2D uv_texture;
 )",
-		.body = R"(
+		R"(
 	float y = texture2D(y_texture, v_texcoord).a - 0.0625;
 	vec2 uv = texture2D(uv_texture, v_texcoord).rg - vec2(0.5, 0.5);
 	float u = uv.x;
@@ -124,10 +124,10 @@ uniform sampler2D uv_texture;
 
 ShaderPart FragmentGlobalOpacity() {
 	return {
-		.header = R"(
+		R"(
 uniform float g_opacity;
 )",
-		.body = R"(
+		R"(
 	result *= g_opacity;
 )",
 	};
@@ -135,7 +135,7 @@ uniform float g_opacity;
 
 ShaderPart VertexViewportTransform() {
 	return {
-		.header = R"(
+		R"(
 uniform vec2 viewport;
 vec4 transform(vec4 position) {
 	return vec4(
@@ -144,7 +144,7 @@ vec4 transform(vec4 position) {
 		position.w);
 }
 )",
-		.body = R"(
+		R"(
 	result = transform(result);
 )",
 	};
@@ -152,7 +152,7 @@ vec4 transform(vec4 position) {
 
 ShaderPart FragmentRoundCorners() {
 	return {
-		.header = R"(
+		R"(
 uniform vec4 roundRect;
 uniform vec2 radiusOutline;
 uniform vec4 roundBg;
@@ -173,7 +173,7 @@ vec2 roundedCorner() {
 		1. - (smoothstep(0., 1., outline) * outlineFg.a));
 }
 )",
-		.body = R"(
+		R"(
 	vec2 roundOutline = roundedCorner();
 	result = result * roundOutline.y
 		+ vec4(outlineFg.rgb, 1) * (1. - roundOutline.y);
@@ -184,10 +184,10 @@ vec2 roundedCorner() {
 
 ShaderPart FragmentStaticColor() {
 	return {
-		.header = R"(
+		R"(
 uniform vec4 s_color;
 )",
-		.body = R"(
+		R"(
 	result = s_color;
 )",
 	};
diff --git a/ui/image/image_prepare.cpp b/ui/image/image_prepare.cpp
index 2130b986..94993ebf 100644
--- a/ui/image/image_prepare.cpp
+++ b/ui/image/image_prepare.cpp
@@ -22,6 +22,11 @@
 #include <QtGui/QImageReader>
 #include <QtSvg/QSvgRenderer>
 
+// On the v141_xp toolchain the patched SDK 7.1A rpcndr.h already typedefs
+// `boolean` (as unsigned char), so libjpeg's jmorecfg.h must not re-typedef it.
+#ifndef HAVE_BOOLEAN
+#define HAVE_BOOLEAN
+#endif
 #include <jpeglib.h>
 #include <setjmp.h>
 
@@ -1420,7 +1425,7 @@ QByteArray ExpandInlineBytes(const QByteArray &bytes) {
 }
 
 QImage FromInlineBytes(const QByteArray &bytes) {
-	return Read({ .content = ExpandInlineBytes(bytes) }).image;
+	return Read({ {}, ExpandInlineBytes(bytes) }).image; // path, content
 }
 
 // Thanks TDLib for code.
diff --git a/ui/image/image_prepare.h b/ui/image/image_prepare.h
index 6368c0e6..a6159925 100644
--- a/ui/image/image_prepare.h
+++ b/ui/image/image_prepare.h
@@ -72,21 +72,28 @@ struct CornersMaskRef {
 
 	std::array<const QImage*, 4> p{};
 
-	friend inline constexpr std::strong_ordering operator<=>(
+	friend inline bool operator==(
+			CornersMaskRef a,
+			CornersMaskRef b) noexcept {
+		return a.p == b.p;
+	}
+	friend inline bool operator!=(
+			CornersMaskRef a,
+			CornersMaskRef b) noexcept {
+		return a.p != b.p;
+	}
+	friend inline bool operator<(
 			CornersMaskRef a,
 			CornersMaskRef b) noexcept {
 		for (auto i = 0; i != 4; ++i) {
 			if (a.p[i] < b.p[i]) {
-				return std::strong_ordering::less;
+				return true;
 			} else if (a.p[i] > b.p[i]) {
-				return std::strong_ordering::greater;
+				return false;
 			}
 		}
-		return std::strong_ordering::equal;
+		return false;
 	}
-	friend inline constexpr bool operator==(
-		CornersMaskRef a,
-		CornersMaskRef b) noexcept = default;
 };
 
 [[nodiscard]] const std::array<QImage, 4> &CornersMask(
diff --git a/ui/layers/show.cpp b/ui/layers/show.cpp
index f28c64bf..d66ae23e 100644
--- a/ui/layers/show.cpp
+++ b/ui/layers/show.cpp
@@ -46,15 +46,22 @@ base::weak_ptr<Instance> Show::showToast(Config &&config) const {
 base::weak_ptr<Instance> Show::showToast(
 		TextWithEntities &&text,
 		crl::time duration) const {
-	return showToast({ .text = std::move(text), .duration = duration });
+	return showToast({
+		{}, // title
+		std::move(text), // text
+		&st::defaultMultilineToast, // st
+		duration, // duration
+	});
 }
 
 base::weak_ptr<Instance> Show::showToast(
 		const QString &text,
 		crl::time duration) const {
 	return showToast({
-		.text = TextWithEntities{ text },
-		.duration = duration,
+		{}, // title
+		TextWithEntities{ text }, // text
+		&st::defaultMultilineToast, // st
+		duration, // duration
 	});
 }
 
diff --git a/ui/paint/arcs.cpp b/ui/paint/arcs.cpp
index 2f346e41..1870a827 100644
--- a/ui/paint/arcs.cpp
+++ b/ui/paint/arcs.cpp
@@ -42,9 +42,10 @@ void ArcsAnimation::initArcs(std::vector<float> thresholds) {
 		const auto threshold = thresholds[i];
 		const auto progress = (threshold > _currentValue) ? 1. : 0.;
 		auto arc = Arc{
-			.rect = computeArcRect(i + 1),
-			.threshold = threshold,
-			.progress = progress,
+			computeArcRect(i + 1),
+			threshold,
+			0, // startTime (skipped by upstream designated init)
+			progress,
 		};
 		_arcs.push_back(std::move(arc));
 	}
diff --git a/ui/platform/win/ui_utility_win.cpp b/ui/platform/win/ui_utility_win.cpp
index 10bef5c2..e0cdcb4c 100644
--- a/ui/platform/win/ui_utility_win.cpp
+++ b/ui/platform/win/ui_utility_win.cpp
@@ -49,6 +49,9 @@ std::optional<bool> IsOverlapped(
 	const auto handle = HWND(widget->winId());
 	Expects(handle != nullptr);
 
+	// XP walk: IVirtualDesktopManager is a Windows 10 shell interface absent
+	// from the XP target SDK; skip the virtual-desktop check on old Windows.
+#ifdef __IVirtualDesktopManager_INTERFACE_DEFINED__
 	ComPtr<IVirtualDesktopManager> virtualDesktopManager;
 	HRESULT hr = CoCreateInstance(
 		CLSID_VirtualDesktopManager,
@@ -65,6 +68,7 @@ std::optional<bool> IsOverlapped(
 			return true;
 		}
 	}
+#endif // __IVirtualDesktopManager_INTERFACE_DEFINED__
 
 	const auto nativeRect = [&] {
 		const auto topLeft = [&] {
diff --git a/ui/platform/win/ui_window_shadow_win.cpp b/ui/platform/win/ui_window_shadow_win.cpp
index 2b72124d..1a2f0bea 100644
--- a/ui/platform/win/ui_window_shadow_win.cpp
+++ b/ui/platform/win/ui_window_shadow_win.cpp
@@ -328,6 +328,13 @@ Gdiplus::Pen WindowShadow::getPen(uchar alpha) const {
 }
 
 void WindowShadow::update(Changes changes, WINDOWPOS *pos) {
+	// XP walk: the custom drop-shadow (four WS_EX_LAYERED windows updated via
+	// UpdateLayeredWindow) blocks the main thread during the first frame-change /
+	// window show on XP -- the window never paints. DWM draws the shadow on Win8+,
+	// so drop the custom one when there is no composition.
+	if (!::Platform::IsWindows8OrGreater()) {
+		return;
+	}
 	if (!working()) {
 		return;
 	}
diff --git a/ui/platform/win/ui_window_title_win.cpp b/ui/platform/win/ui_window_title_win.cpp
index 7c66f780..f521dceb 100644
--- a/ui/platform/win/ui_window_title_win.cpp
+++ b/ui/platform/win/ui_window_title_win.cpp
@@ -22,7 +22,14 @@
 #include <QtGui/QWindow>
 
 #include <windows.h>
-#include <shellscalingapi.h>
+// XP walk: <shellscalingapi.h> (Per-Monitor DPI, Win8.1+) does not parse against
+// the v141_xp SDK at _WIN32_WINNT=0x0501 (its NTDDI-gated decls reference undefined
+// types). The DPI-scale path below is only reached for Win11 rounded-corner windows
+// (never on XP) and GetScaleFactorForMonitor is GetProcAddress-resolved from
+// Shcore.dll (absent on XP); declare just the DEVICE_SCALE_FACTOR enum we need.
+enum DEVICE_SCALE_FACTOR {
+	DEVICE_SCALE_FACTOR_INVALID = 0,
+};
 
 namespace Ui {
 namespace Platform {
@@ -48,7 +55,8 @@ namespace internal {
 
 TitleControls::Layout TitleControlsLayout() {
 	return TitleControls::Layout{
-		.right = {
+		{}, // left
+		{ // right
 			TitleControls::Control::Minimize,
 			TitleControls::Control::Maximize,
 			TitleControls::Control::Close,
@@ -141,9 +149,19 @@ void TitleWidget::setResizeEnabled(bool enabled) {
 
 void TitleWidget::paintEvent(QPaintEvent *e) {
 	const auto active = window()->isActiveWindow();
-	QPainter(this).fillRect(
+	auto p = QPainter(this);
+	p.fillRect(
 		e->rect(),
 		active ? _controls.st()->bgActive : _controls.st()->bg);
+	// XP walk: the old app-level window_title_win.cpp drew a red build watermark in
+	// the title bar; v2.9.0 moved the title bar into this shared widget. Re-draw the
+	// window title in red so the deterministic VM screenshot can read the running
+	// port version (the app sets the title to "Telegram [XP x.y.z #1]").
+	p.setPen(QColor(200, 0, 0));
+	p.drawText(
+		QRect(10, 0, width() - _controls.geometry().width() - 20, height()),
+		Qt::AlignLeft | Qt::AlignVCenter,
+		window()->windowTitle());
 }
 
 void TitleWidget::resizeEvent(QResizeEvent *e) {
@@ -190,7 +208,7 @@ void TitleWidget::refreshAdditionalPaddings(HWND handle) {
 		return;
 	}
 	auto placement = WINDOWPLACEMENT{
-		.length = sizeof(WINDOWPLACEMENT),
+		sizeof(WINDOWPLACEMENT),
 	};
 	if (!GetWindowPlacement(handle, &placement)) {
 		LOG(("System Error: GetWindowPlacement failed."));
diff --git a/ui/platform/win/ui_window_win.cpp b/ui/platform/win/ui_window_win.cpp
index faa5d0f7..ea6a7ecd 100644
--- a/ui/platform/win/ui_window_win.cpp
+++ b/ui/platform/win/ui_window_win.cpp
@@ -34,6 +34,11 @@
 #include <uxtheme.h>
 #include <windowsx.h>
 
+// WM_DPICHANGED (Windows 8.1+) is absent from the XP target SDK headers.
+#ifndef WM_DPICHANGED
+#define WM_DPICHANGED 0x02E0
+#endif // WM_DPICHANGED
+
 Q_DECLARE_METATYPE(QMargins);
 
 namespace Ui::Platform {
@@ -89,8 +94,22 @@ BOOL(__stdcall *AdjustWindowRectExForDpi)(
 }
 
 [[nodiscard]] bool IsCompositionEnabled() {
+	// On Windows XP dwmapi.dll does not exist, so the delay-loaded
+	// DwmIsCompositionEnabled import raises a fatal proc-not-found exception
+	// the first time it is called. Resolve it dynamically instead and treat a
+	// missing library as "composition disabled" (degrade, never crash).
+	static const auto kIsCompositionEnabled = [] {
+		using FnType = HRESULT(WINAPI*)(BOOL*);
+		auto fn = FnType(nullptr);
+		const auto dwmapi = base::Platform::SafeLoadLibrary(L"dwmapi.dll");
+		base::Platform::LoadMethod(dwmapi, "DwmIsCompositionEnabled", fn);
+		return fn;
+	}();
+	if (!kIsCompositionEnabled) {
+		return false;
+	}
 	auto result = BOOL(FALSE);
-	const auto success = (DwmIsCompositionEnabled(&result) == S_OK);
+	const auto success = (kIsCompositionEnabled(&result) == S_OK);
 	return success && result;
 }
 
@@ -487,6 +506,7 @@ void WindowHelper::init() {
 	initialShadowUpdate();
 	updateCornersRounding();
 
+#if UI_HAS_DIRECT_MANIPULATION
 	auto dm = std::make_unique<DirectManipulation>(window());
 	if (dm->valid()) {
 		_directManipulation = std::move(dm);
@@ -495,28 +515,7 @@ void WindowHelper::init() {
 			handleDirectManipulationEvent(event);
 		}, window()->lifetime());
 	}
-
-	window()->shownValue() | rpl::filter([=](bool shown) {
-		return !shown;
-	}) | rpl::start_with_next([=] {
-		updateCloaking();
-
-		const auto qwindow = window()->windowHandle();
-		const auto firstPaintEventFilter = std::make_shared<QObject*>();
-		*firstPaintEventFilter = base::install_event_filter(
-			qwindow,
-			[=](not_null<QEvent*> e) {
-				if (e->type() == QEvent::Expose && qwindow->isExposed()) {
-					InvokeQueued(qwindow, [=] {
-						InvokeQueued(qwindow, [=] {
-							updateCloaking();
-						});
-					});
-					delete base::take(*firstPaintEventFilter);
-				}
-				return base::EventFilterResult::Continue;
-			});
-	}, window()->lifetime());
+#endif // UI_HAS_DIRECT_MANIPULATION
 }
 
 void WindowHelper::handleDirectManipulationEvent(
@@ -666,7 +665,7 @@ bool WindowHelper::handleNativeEvent(
 	case WM_WINDOWPOSCHANGING:
 	case WM_WINDOWPOSCHANGED: {
 		auto placement = WINDOWPLACEMENT{
-			.length = sizeof(WINDOWPLACEMENT),
+			sizeof(WINDOWPLACEMENT), // length
 		};
 		if (!GetWindowPlacement(_handle, &placement)) {
 			LOG(("System Error: GetWindowPlacement failed."));
@@ -748,7 +747,7 @@ bool WindowHelper::handleNativeEvent(
 				return DefWindowProc(_handle, msg, wParam, lParam);
 			}
 			auto request = HitTestRequest{
-				.point = mapped,
+				mapped,
 			};
 			_hitTestRequests.fire(&request);
 			switch (const auto result = request.result) {
@@ -781,12 +780,14 @@ bool WindowHelper::handleNativeEvent(
 		});
 	} return false;
 
+#if UI_HAS_DIRECT_MANIPULATION
 	case DM_POINTERHITTEST: {
 		if (_directManipulation) {
 			_directManipulation->handlePointerHitTest(wParam);
 			return true;
 		}
 	} return false;
+#endif // UI_HAS_DIRECT_MANIPULATION
 
 	}
 	return false;
diff --git a/ui/platform/win/ui_windows_direct_manipulation.cpp b/ui/platform/win/ui_windows_direct_manipulation.cpp
index 885c8cf2..3b6c473e 100644
--- a/ui/platform/win/ui_windows_direct_manipulation.cpp
+++ b/ui/platform/win/ui_windows_direct_manipulation.cpp
@@ -13,6 +13,8 @@
 
 namespace Ui::Platform {
 
+#if UI_HAS_DIRECT_MANIPULATION
+
 class DirectManipulation::Handler
 	: public IDirectManipulationViewportEventHandler
 	, public IDirectManipulationInteractionEventHandler {
@@ -431,4 +433,26 @@ void DirectManipulation::destroy() {
 	}
 }
 
+#else // UI_HAS_DIRECT_MANIPULATION
+
+DirectManipulation::DirectManipulation(not_null<RpWidget*> widget)
+: _handle(GetWindowHandle(widget)) {
+}
+
+DirectManipulation::~DirectManipulation() {
+}
+
+bool DirectManipulation::valid() const {
+	return false;
+}
+
+auto DirectManipulation::events() const -> rpl::producer<Event> {
+	return _events.events();
+}
+
+void DirectManipulation::handlePointerHitTest(WPARAM) {
+}
+
+#endif // UI_HAS_DIRECT_MANIPULATION
+
 }  // namespace Ui::Platform
diff --git a/ui/platform/win/ui_windows_direct_manipulation.h b/ui/platform/win/ui_windows_direct_manipulation.h
index 716b22f3..57704c2b 100644
--- a/ui/platform/win/ui_windows_direct_manipulation.h
+++ b/ui/platform/win/ui_windows_direct_manipulation.h
@@ -11,8 +11,19 @@
 
 #include <QtCore/QPoint>
 
-#include <windows.h>
+#include <windows.h> // XP walk: v4.11.8 added explicit windows.h; keep + my gate below.
+
+// XP walk: DirectManipulation (IDirectManipulationManager) is Win8+; gate it so the
+// v141_xp build compiles (XP path: UI_HAS_DIRECT_MANIPULATION 0).
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT >= 0x0602)
+#define UI_HAS_DIRECT_MANIPULATION 1
+#else // _WIN32_WINNT >= 0x0602
+#define UI_HAS_DIRECT_MANIPULATION 0
+#endif // _WIN32_WINNT >= 0x0602
+
+#if UI_HAS_DIRECT_MANIPULATION
 #include <directmanipulation.h>
+#endif // UI_HAS_DIRECT_MANIPULATION
 
 namespace Ui {
 class RpWidget;
@@ -48,6 +59,7 @@ public:
 	[[nodiscard]] rpl::producer<Event> events() const;
 
 private:
+#if UI_HAS_DIRECT_MANIPULATION
 	class Handler;
 
 	bool init(not_null<RpWidget*> widget);
@@ -64,6 +76,10 @@ private:
 	Ui::Animations::Basic _interacting;
 	rpl::event_stream<Event> _events;
 	rpl::lifetime _lifetime;
+#else // UI_HAS_DIRECT_MANIPULATION
+	HWND _handle = nullptr;
+	rpl::event_stream<Event> _events;
+#endif // UI_HAS_DIRECT_MANIPULATION
 
 };
 
diff --git a/ui/style/style_core.h b/ui/style/style_core.h
index aed0de34..bfcd34fb 100644
--- a/ui/style/style_core.h
+++ b/ui/style/style_core.h
@@ -92,3 +92,14 @@ inline QImage createInvertedCircleMask(int size) {
 }
 
 } // namespace style
+
+// <windows.h> (via Qt, no WIN32_LEAN_AND_MEAN) drags in rpcndr.h's
+// `#define small char` / `#define hyper __int64`. Generated style structs have
+// members named `small`; generated style headers include this one right before
+// those structs, so undefining here keeps the identifiers usable.
+#ifdef small
+#undef small
+#endif
+#ifdef hyper
+#undef hyper
+#endif
diff --git a/ui/style/style_core_font.cpp b/ui/style/style_core_font.cpp
index 626ba058..f23cec60 100644
--- a/ui/style/style_core_font.cpp
+++ b/ui/style/style_core_font.cpp
@@ -178,10 +178,10 @@ struct Metrics {
 	const auto startSize = font.pixelSize();
 	const auto metrics = QFontMetricsF(font);
 	const auto simple = [&] {
-		return Metrics{
-			.pixelSize = startSize,
-			.ascent = metrics.ascent(),
-			.height = metrics.height(),
+		return Metrics{ // XP walk: designated -> positional (C7555)
+			startSize, // pixelSize
+			metrics.ascent(), // ascent
+			metrics.height(), // height
 		};
 	};
 
@@ -253,10 +253,10 @@ struct Metrics {
 		const auto desiredTightHeight = desiredFull.height();
 		const auto heightAdd = basicMetrics.height() - desiredTightHeight;
 		const auto tightHeight = full.height();
-		return Metrics{
-			.pixelSize = size,
-			.ascent = basicMetrics.ascent(),
-			.height = tightHeight + heightAdd,
+		return Metrics{ // XP walk: designated -> positional (C7555)
+			size, // pixelSize
+			basicMetrics.ascent(), // ascent (v5.0.1: simplified from tightAscent+ascentAdd)
+			tightHeight + heightAdd, // height
 		};
 	};
 
@@ -352,16 +352,17 @@ struct Metrics {
 	font.setUnderline(flags & FontFlag::Underline);
 	font.setStrikeOut(flags & FontFlag::StrikeOut);
 
+	// XP walk: metrics computed above (v5.0.1 moved it before weight/style).
 	const auto index = (family == Custom) ? 0 : RegisterFontFamily(family);
-	return {
-		.font = font,
-		.ascent = metrics.ascent,
-		.height = metrics.height,
-		.iascent = int(base::SafeRound(metrics.ascent)),
-		.iheight = int(base::SafeRound(metrics.height)),
-		.requestedFamily = index,
-		.requestedSize = size,
-		.requestedFlags = flags,
+	return { // XP walk: designated -> positional (C7555)
+		font, // font
+		metrics.ascent, // ascent
+		metrics.height, // height
+		int(base::SafeRound(metrics.ascent)), // iascent
+		int(base::SafeRound(metrics.height)), // iheight
+		index, // requestedFamily (v5.0.1: Custom-font -> 0)
+		size, // requestedSize
+		flags, // requestedFlags
 	};
 }
 
diff --git a/ui/style/style_core_palette.cpp b/ui/style/style_core_palette.cpp
index bad3e65c..af204c62 100644
--- a/ui/style/style_core_palette.cpp
+++ b/ui/style/style_core_palette.cpp
@@ -194,7 +194,7 @@ auto palette::PrepareFinalizeHelper(const colorizer &with)
 		return nullptr;
 	}
 	auto result = std::make_unique<FinalizeHelper>(FinalizeHelper{
-		.with = &with,
+		&with,
 	});
 	result->ignoreKeys.reserve(with.ignoreKeys.size() + 1);
 	result->ignoreKeys.emplace(0);
diff --git a/ui/text/custom_emoji_instance.cpp b/ui/text/custom_emoji_instance.cpp
index 1e1acac1..fa24e1f0 100644
--- a/ui/text/custom_emoji_instance.cpp
+++ b/ui/text/custom_emoji_instance.cpp
@@ -98,7 +98,7 @@ Preview::Preview(QPainterPath path, float64 scale)
 }
 
 Preview::Preview(QImage image, bool exact)
-: _data(Image{ .data = std::move(image), .exact = exact }) {
+: _data(Image{ std::move(image), exact }) {
 }
 
 void Preview::paint(QPainter &p, const Context &context) {
@@ -108,7 +108,7 @@ void Preview::paint(QPainter &p, const Context &context) {
 		const auto &data = image->data;
 		const auto factor = style::DevicePixelRatio();
 		const auto rect = QRect(context.position, data.size() / factor);
-		PaintScaledImage(p, rect, { .image = &data }, context);
+		PaintScaledImage(p, rect, { &data }, context);
 	}
 }
 
@@ -220,9 +220,9 @@ QByteArray Cache::serialize() {
 	Expects(_full.bytesPerLine() == sizeof(int32) * _full.width());
 
 	auto header = CacheHeader{
-		.version = kCacheVersion,
-		.size = _size,
-		.frames = _frames,
+		kCacheVersion,
+		_size,
+		_frames,
 	};
 	const auto input = _full.width() * _full.height() * sizeof(int32);
 	const auto max = sizeof(CacheHeader)
@@ -408,9 +408,9 @@ PaintFrameResult Cache::paintCurrentFrame(
 	PaintScaledImage(p, rect, info, context);
 	const auto next = first ? 0 : currentFrameFinishes();
 	return {
-		.painted = true,
-		.next = next,
-		.duration = next ? (next - _shown) : 0,
+		true,
+		next,
+		next ? (next - _shown) : 0,
 	};
 }
 
@@ -777,7 +777,7 @@ void Instance::setColored() {
 	if (!_colored) {
 		_colored = true;
 		if (ready()) {
-			_repaintLater(this, { .when = crl::now() + 1 });
+			_repaintLater(this, { crl::now() + 1 });
 		}
 	}
 }
diff --git a/ui/text/text.cpp b/ui/text/text.cpp
index 0a72ef82..3095d104 100644
--- a/ui/text/text.cpp
+++ b/ui/text/text.cpp
@@ -114,8 +114,8 @@ not_null<SpoilerMessCached*> SpoilerMessCache::lookup(QColor color) {
 	}
 	Assert(_cache.size() < _capacity);
 	_cache.push_back({
-		.mess = Ui::SpoilerMessCached(DefaultTextSpoilerMask(), color),
-		.color = color,
+		Ui::SpoilerMessCached(DefaultTextSpoilerMask(), color),
+		color,
 	});
 	return &_cache.back().mess;
 }
@@ -152,15 +152,17 @@ GeometryDescriptor SimpleGeometry(
 	};
 
 	// Try to minimize captured values (to minimize Fn allocations).
+	// XP walk: LineGeometry designated inits -> positional (C7555). {left, width, elided}.
 	if (!elisionLines) {
 		return wrap([=](int line) {
-			return LineGeometry{ .width = availableWidth };
+			return LineGeometry{ 0, availableWidth }; // left, width
 		});
 	} else if (!elisionRemoveFromEnd) {
 		return wrap([=](int line) {
 			return LineGeometry{
-				.width = availableWidth,
-				.elided = (line + 1 >= elisionLines),
+				0, // left
+				availableWidth, // width
+				(line + 1 >= elisionLines), // elided
 			};
 		}, elisionBreakEverywhere);
 	} else {
@@ -168,8 +170,9 @@ GeometryDescriptor SimpleGeometry(
 			const auto elided = (line + 1 >= elisionLines);
 			const auto removeFromEnd = (elided ? elisionRemoveFromEnd : 0);
 			return LineGeometry{
-				.width = availableWidth - removeFromEnd,
-				.elided = elided,
+				0, // left
+				availableWidth - removeFromEnd, // width
+				elided, // elided
 			};
 		}, elisionBreakEverywhere);
 	}
@@ -880,9 +883,11 @@ void String::insertModifications(int position, int delta) {
 		++i->skipped;
 	} else {
 		modifications.insert(i, {
-			.position = position,
-			.skipped = uint16(delta < 0 ? (-delta) : 0),
-			.added = (delta > 0),
+			// XP walk: designated -> positional (C7555). Modification{position,
+			// skipped, added}.
+			position, // position
+			uint16(delta < 0 ? (-delta) : 0), // skipped
+			(delta > 0), // added
 		});
 	}
 }
@@ -1210,48 +1215,84 @@ StateResult String::getState(
 void String::draw(Painter &p, int32 left, int32 top, int32 w, style::align align, int32 yFrom, int32 yTo, TextSelection selection, bool fullWidthSelection) const {
 //	p.fillRect(QRect(left, top, w, countHeight(w)), QColor(0, 0, 0, 32)); // debug
 	Renderer(*this).draw(p, {
-		.position = { left, top },
-		.availableWidth = w,
-		.align = align,
-		.clip = (yTo >= 0
+		// XP walk: positional PaintContext updated for new layout (geometry(4),
+		// pre/blockquote/colors(8-10) inserted).
+		{ left, top }, // position
+		{}, // outerWidth
+		w, // availableWidth
+		{}, // geometry
+		align, // align
+		(yTo >= 0
 			? QRect(left, top + yFrom, w, yTo - yFrom)
-			: QRect()),
-		.palette = &p.textPalette(),
-		.paused = p.inactive(),
-		.selection = selection,
-		.fullWidthSelection = fullWidthSelection,
+			: QRect()), // clip
+		&p.textPalette(), // palette
+		{}, // pre
+		{}, // blockquote
+		{}, // colors
+		{}, // spoiler
+		{}, // now
+		p.inactive(), // paused
+		{}, // pausedEmoji
+		{}, // pausedSpoiler
+		selection, // selection
+		fullWidthSelection, // fullWidthSelection
 	});
 }
 
 void String::drawElided(Painter &p, int32 left, int32 top, int32 w, int32 lines, style::align align, int32 yFrom, int32 yTo, int32 removeFromEnd, bool breakEverywhere, TextSelection selection) const {
 //	p.fillRect(QRect(left, top, w, countHeight(w)), QColor(0, 0, 0, 32)); // debug
 	Renderer(*this).draw(p, {
-		.position = { left, top },
-		.availableWidth = w,
-		.align = align,
-		.clip = (yTo >= 0
+		// XP walk: designated init -> positional (C7555). PaintContext layout:
+		// ...selection(16), fullWidthSelection(17), highlight(18), elisionHeight(19),
+		// elisionLines(20 -- v4.11.4 NEW, replaced elisionOneLine), elisionRemoveFromEnd(21).
+		{ left, top }, // position
+		{}, // outerWidth
+		w, // availableWidth
+		{}, // geometry
+		align, // align
+		(yTo >= 0
 			? QRect(left, top + yFrom, w, yTo - yFrom)
-			: QRect()),
-		.palette = &p.textPalette(),
-		.paused = p.inactive(),
-		.selection = selection,
-		.elisionLines = lines,
-		.elisionRemoveFromEnd = removeFromEnd,
+			: QRect()), // clip
+		&p.textPalette(), // palette
+		{}, // pre
+		{}, // blockquote
+		{}, // colors
+		{}, // spoiler
+		{}, // now
+		p.inactive(), // paused
+		{}, // pausedEmoji
+		{}, // pausedSpoiler
+		selection, // selection
+		true, // fullWidthSelection (default)
+		{}, // highlight
+		{}, // elisionHeight (v4.11.4 drawElided uses elisionLines instead)
+		lines, // elisionLines -- XP walk: v4.11.4 replaced elisionOneLine with elisionLines(int)
+		removeFromEnd, // elisionRemoveFromEnd
 	});
 }
 
 void String::drawLeft(Painter &p, int32 left, int32 top, int32 width, int32 outerw, style::align align, int32 yFrom, int32 yTo, TextSelection selection) const {
 	Renderer(*this).draw(p, {
-		.position = { left, top },
-		//.outerWidth = outerw,
-		.availableWidth = width,
-		.align = align,
-		.clip = (yTo >= 0
+		// XP walk: designated init -> positional (C7555); geometry(4) and
+		// pre/blockquote/colors(8-10) inserted vs the old PaintContext layout.
+		{ left, top }, // position
+		{}, // outerWidth
+		width, // availableWidth
+		{}, // geometry
+		align, // align
+		(yTo >= 0
 			? QRect(left, top + yFrom, width, yTo - yFrom)
-			: QRect()),
-		.palette = &p.textPalette(),
-		.paused = p.inactive(),
-		.selection = selection,
+			: QRect()), // clip
+		&p.textPalette(), // palette
+		{}, // pre
+		{}, // blockquote
+		{}, // colors
+		{}, // spoiler
+		{}, // now
+		p.inactive(), // paused
+		{}, // pausedEmoji
+		{}, // pausedSpoiler
+		selection, // selection
 	});
 }
 
@@ -1616,7 +1657,7 @@ OnlyCustomEmoji String::toOnlyCustomEmoji() const {
 		if (raw->type() == TextBlockType::CustomEmoji) {
 			const auto custom = static_cast<const CustomEmojiBlock*>(raw);
 			result.lines.back().push_back({
-				.entityData = custom->_custom->entityData(),
+				custom->_custom->entityData(),
 			});
 		} else if (raw->type() == TextBlockType::Newline) {
 			result.lines.emplace_back();
diff --git a/ui/text/text.h b/ui/text/text.h
index 18d668ca..06a37a61 100644
--- a/ui/text/text.h
+++ b/ui/text/text.h
@@ -191,10 +191,11 @@ void ValidateQuotePaintCache(
 	const style::QuoteStyle &st);
 
 struct SkipBlockPaintParts {
-	uint32 skippedTop : 29 = 0;
-	uint32 skipBottom : 1 = 0;
-	uint32 expandIcon : 1 = 0;
-	uint32 collapseIcon : 1 = 0;
+	// XP walk: bitfield packing + default-member-inits dropped (C7582, C++20-only).
+	uint32 skippedTop = 0;
+	uint32 skipBottom = 0;
+	uint32 expandIcon = 0;
+	uint32 collapseIcon = 0;
 };
 void FillQuotePaint(
 	QPainter &p,
@@ -221,7 +222,7 @@ struct PaintContext {
 	const style::TextPalette *palette = nullptr;
 	QuotePaintCache *pre = nullptr;
 	QuotePaintCache *blockquote = nullptr;
-	std::span<SpecialColor> colors;
+	gsl::span<SpecialColor> colors; // XP walk: std::span (C++20) -> gsl::span
 	SpoilerMessCache *spoiler = nullptr;
 	crl::time now = 0;
 	bool paused = false;
@@ -462,14 +463,16 @@ private:
 	int _maxWidth = 0;
 	int _minHeight = 0;
 	uint16 _startQuoteIndex = 0;
-	bool _startParagraphLTR : 1 = false;
-	bool _startParagraphRTL : 1 = false;
-	bool _hasCustomEmoji : 1 = false;
-	bool _isIsolatedEmoji : 1 = false;
-	bool _isOnlyCustomEmoji : 1 = false;
-	bool _hasNotEmojiAndSpaces : 1 = false;
-	bool _skipBlockAddedNewline : 1 = false;
-	bool _endsWithQuote : 1 = false;
+	// XP walk: bitfields dropped (`: 1 = false` is a C++20 default-member-init
+	// the v141_xp C++17 toolset rejects).
+	bool _startParagraphLTR = false;
+	bool _startParagraphRTL = false;
+	bool _hasCustomEmoji = false;
+	bool _isIsolatedEmoji = false;
+	bool _isOnlyCustomEmoji = false;
+	bool _hasNotEmojiAndSpaces = false;
+	bool _skipBlockAddedNewline = false;
+	bool _endsWithQuote = false;
 
 	friend class Parser;
 	friend class Renderer;
diff --git a/ui/text/text_block.h b/ui/text/text_block.h
index 0eee5e7c..d3fc68e5 100644
--- a/ui/text/text_block.h
+++ b/ui/text/text_block.h
@@ -80,8 +80,10 @@ protected:
 		uint16 colorIndex);
 
 	uint16 _position = 0;
-	uint16 _type : 4 = 0;
-	uint16 _flags : 12 = 0;
+	// XP walk: bitfield default-inits dropped (C7582, C++20-only); the
+	// AbstractBlock ctor always sets _type/_flags, so the =0 was redundant.
+	uint16 _type : 4;
+	uint16 _flags : 12;
 	uint16 _linkIndex = 0;
 	uint16 _colorIndex = 0;
 
@@ -116,8 +118,10 @@ public:
 
 private:
 	uint16 _quoteIndex = 0;
-	bool _paragraphLTR : 1 = false;
-	bool _paragraphRTL : 1 = false;
+	// XP walk: bitfield packing dropped (C7582); ctor doesn't set these, so the
+	// = false default-init must be preserved.
+	bool _paragraphLTR = false;
+	bool _paragraphRTL = false;
 
 	friend class String;
 	friend class Parser;
diff --git a/ui/text/text_entity.cpp b/ui/text/text_entity.cpp
index 3d40068f..fdcad58f 100644
--- a/ui/text/text_entity.cpp
+++ b/ui/text/text_entity.cpp
@@ -1552,9 +1552,9 @@ MentionNameFields MentionNameDataToFields(QStringView data) {
 		return {};
 	}
 	return {
-		.selfId = parts[1].toULongLong(),
-		.userId = components[0].toULongLong(),
-		.accessHash = parts[0].toULongLong(),
+		parts[1].toULongLong(),
+		components[0].toULongLong(),
+		parts[0].toULongLong(),
 	};
 }
 
@@ -2067,8 +2067,8 @@ EntitiesInText ConvertTextTagsToEntities(const TextWithTags::Tags &tags) {
 	struct State {
 		QString link;
 		QString language;
-		uint32 mask : 31 = 0;
-		uint32 collapsed : 1 = 0;
+		uint32 mask = 0; // XP walk: bit-field packing dropped (C7582, C++20-only)
+		uint32 collapsed = 0;
 
 		void set(EntityType type) {
 			mask |= (1 << int(type));
@@ -2339,7 +2339,7 @@ void SetClipboardText(
 
 TextForMimeData TextForMimeData::WithExpandedLinks(
 		const TextWithEntities &text) {
-	auto result = TextForMimeData{ .rich = text };
+	auto result = TextForMimeData{ {}, text };
 	if (!ranges::contains(
 			text.entities,
 			EntityType::CustomUrl,
diff --git a/ui/text/text_entity.h b/ui/text/text_entity.h
index f6ba110c..daa135a4 100644
--- a/ui/text/text_entity.h
+++ b/ui/text/text_entity.h
@@ -52,12 +52,20 @@ struct EntityLinkData {
 	EntityType type = EntityType::Invalid;
 	EntityLinkShown shown = EntityLinkShown::Full;
 
-	friend inline auto operator<=>(
-		const EntityLinkData &,
-		const EntityLinkData &) = default;
-	friend inline bool operator==(
-		const EntityLinkData &,
-		const EntityLinkData &) = default;
+	// XP walk: defaulted comparisons (C7589, C++20) -> explicit ==/!=.
+	[[nodiscard]] friend inline bool operator==(
+			const EntityLinkData &a,
+			const EntityLinkData &b) {
+		return (a.text == b.text)
+			&& (a.data == b.data)
+			&& (a.type == b.type)
+			&& (a.shown == b.shown);
+	}
+	[[nodiscard]] friend inline bool operator!=(
+			const EntityLinkData &a,
+			const EntityLinkData &b) {
+		return !(a == b);
+	}
 };
 
 class EntityInText;
@@ -121,12 +129,20 @@ public:
 		return type() != EntityType::Invalid;
 	}
 
-	friend inline auto operator<=>(
-		const EntityInText &,
-		const EntityInText &) = default;
-	friend inline bool operator==(
-		const EntityInText &,
-		const EntityInText &) = default;
+	// XP walk: defaulted comparisons (C7589, C++20) -> explicit ==/!=.
+	[[nodiscard]] friend inline bool operator==(
+			const EntityInText &a,
+			const EntityInText &b) {
+		return (a._type == b._type)
+			&& (a._offset == b._offset)
+			&& (a._length == b._length)
+			&& (a._data == b._data);
+	}
+	[[nodiscard]] friend inline bool operator!=(
+			const EntityInText &a,
+			const EntityInText &b) {
+		return !(a == b);
+	}
 
 private:
 	EntityType _type = EntityType::Invalid;
@@ -187,12 +203,17 @@ struct TextWithEntities {
 		return result;
 	}
 
-	friend inline auto operator<=>(
-		const TextWithEntities &,
-		const TextWithEntities &) = default;
-	friend inline bool operator==(
-		const TextWithEntities &,
-		const TextWithEntities &) = default;
+	// XP walk: defaulted comparisons (C7589, C++20) -> explicit ==/!=.
+	[[nodiscard]] friend inline bool operator==(
+			const TextWithEntities &a,
+			const TextWithEntities &b) {
+		return (a.text == b.text) && (a.entities == b.entities);
+	}
+	[[nodiscard]] friend inline bool operator!=(
+			const TextWithEntities &a,
+			const TextWithEntities &b) {
+		return !(a == b);
+	}
 };
 
 struct TextForMimeData {
@@ -263,8 +284,19 @@ struct TextWithTags {
 		int length = 0;
 		QString id;
 
-		friend inline auto operator<=>(const Tag &, const Tag &) = default;
-		friend inline bool operator==(const Tag &, const Tag &) = default;
+		// XP walk: defaulted comparisons (C7589, C++20) -> explicit ==/!=.
+		[[nodiscard]] friend inline bool operator==(
+				const Tag &a,
+				const Tag &b) {
+			return (a.offset == b.offset)
+				&& (a.length == b.length)
+				&& (a.id == b.id);
+		}
+		[[nodiscard]] friend inline bool operator!=(
+				const Tag &a,
+				const Tag &b) {
+			return !(a == b);
+		}
 	};
 	using Tags = QVector<Tag>;
 
@@ -274,12 +306,17 @@ struct TextWithTags {
 	[[nodiscard]] bool empty() const {
 		return text.isEmpty();
 	}
-	friend inline auto operator<=>(
-		const TextWithTags &,
-		const TextWithTags &) = default;
-	friend inline bool operator==(
-		const TextWithTags &,
-		const TextWithTags &) = default;
+	// XP walk: defaulted comparisons (C7589, C++20) -> explicit ==/!=.
+	[[nodiscard]] friend inline bool operator==(
+			const TextWithTags &a,
+			const TextWithTags &b) {
+		return (a.text == b.text) && (a.tags == b.tags);
+	}
+	[[nodiscard]] friend inline bool operator!=(
+			const TextWithTags &a,
+			const TextWithTags &b) {
+		return !(a == b);
+	}
 };
 
 // Parsing helpers.
diff --git a/ui/text/text_parser.cpp b/ui/text/text_parser.cpp
index 7d41541d..2aa40fb8 100644
--- a/ui/text/text_parser.cpp
+++ b/ui/text/text_parser.cpp
@@ -386,8 +386,16 @@ bool Parser::checkEntities() {
 		} else {
 			flags = TextBlockFlag::Pre;
 			ensureAtNewline({
-				.language = _waitingEntity->data(),
-				.pre = true,
+				// XP walk: designated -> positional (C7555). QuoteDetails: language,
+				// copy, copyWidth, maxWidth, minHeight, scrollLeft, blockquote, pre.
+				_waitingEntity->data(), // language
+				{}, // copy
+				{}, // copyWidth
+				{}, // maxWidth
+				{}, // minHeight
+				{}, // scrollLeft
+				{}, // blockquote
+				true, // pre
 			});
 		}
 		const auto text = QString(entityBegin, entityLength);
@@ -399,14 +407,18 @@ bool Parser::checkEntities() {
 
 		// TODO: remove trimming.
 		if (isSingleLine && (entityType == EntityType::Code)) {
-			_monos.push_back({ .text = text, .type = entityType });
+			_monos.push_back({ text, {}, entityType });
 			monoIndex = _monos.size();
 		}
 	} else if (entityType == EntityType::Blockquote) {
 		flags = TextBlockFlag::Blockquote;
+		// XP walk: designated -> positional (C7555). QuoteDetails order (upstream
+		// v5.1.0 inserted `toggle`@2): language, copy, toggle, copyWidth, maxWidth,
+		// minHeight, scrollLeft, blockquote(7), collapsed(8).
 		ensureAtNewline({
-			.blockquote = true,
-			.collapsed = !_waitingEntity->data().isEmpty(),
+			{}, {}, {}, {}, {}, {}, {}, // 0..6
+			true, // blockquote
+			!_waitingEntity->data().isEmpty(), // collapsed
 		});
 	} else if (entityType == EntityType::Url
 		|| entityType == EntityType::Email
diff --git a/ui/text/text_renderer.cpp b/ui/text/text_renderer.cpp
index ede2e3ef..9dad7aca 100644
--- a/ui/text/text_renderer.cpp
+++ b/ui/text/text_renderer.cpp
@@ -133,8 +133,8 @@ struct Renderer::BidiControl {
 
 FixedRange Intersected(FixedRange a, FixedRange b) {
 	return {
-		.from = std::max(a.from, b.from),
-		.till = std::min(a.till, b.till),
+		std::max(a.from, b.from),
+		std::min(a.till, b.till),
 	};
 }
 
@@ -144,8 +144,8 @@ bool Intersects(FixedRange a, FixedRange b) {
 
 FixedRange United(FixedRange a, FixedRange b) {
 	return {
-		.from = std::min(a.from, b.from),
-		.till = std::max(a.till, b.till),
+		std::min(a.from, b.from),
+		std::max(a.till, b.till),
 	};
 }
 
@@ -463,10 +463,10 @@ void Renderer::fillParagraphBg(int paddingBottom) {
 				ValidateQuotePaintCache(*cache, st);
 			}
 			FillQuotePaint(*_p, rect, *cache, st, {
-				.skippedTop = uint32(top - start),
-				.skipBottom = !isBottom,
-				.expandIcon = cutoff && !_quote->expanded,
-				.collapseIcon = cutoff && _quote->expanded,
+				uint32(top - start), // skippedTop -- XP walk: designated -> positional (C7555)
+				!isBottom, // skipBottom
+				cutoff && !_quote->expanded, // expandIcon
+				cutoff && _quote->expanded, // collapseIcon
 			});
 		}
 		if (cutoff && _quoteExpandLinkLookup
@@ -583,6 +583,8 @@ void Renderer::initNextParagraph(
 }
 
 void Renderer::initNextLine() {
+	// XP walk: v4.11.4 changed GeometryDescriptor::layout to take an int line index
+	// (was LineGeometry) — take theirs; the old designated-init workaround is obsolete.
 	const auto line = _geometry.layout(_lineIndex++);
 	_x = _startLeft + line.left + _quotePadding.left();
 	_startLineWidth = line.width;
@@ -978,9 +980,12 @@ bool Renderer::drawLine(uint16 _lineEnd, const String::TextBlocks::const_iterato
 							: _currentPen)->color();
 						if (!_customEmojiContext) {
 							_customEmojiContext = CustomEmoji::Context{
-								.textColor = color,
-								.now = now(),
-								.paused = _pausedEmoji,
+								color, // textColor
+								{}, // size
+								now(), // now
+								{}, // scale
+								{}, // position
+								_pausedEmoji, // paused
 							};
 							_customEmojiSkip = (st::emojiSize
 								- AdjustCustomEmojiSize(st::emojiSize)) / 2;
diff --git a/ui/text/text_renderer.h b/ui/text/text_renderer.h
index 7668f772..464ba01b 100644
--- a/ui/text/text_renderer.h
+++ b/ui/text/text_renderer.h
@@ -142,7 +142,7 @@ private:
 	SpoilerMessCache *_spoilerCache = nullptr;
 	QPainter *_p = nullptr;
 	const style::TextPalette *_palette = nullptr;
-	std::span<SpecialColor> _colors;
+	gsl::span<SpecialColor> _colors; // XP walk: std::span (C++20) -> gsl::span
 	bool _pausedEmoji = false;
 	bool _pausedSpoiler = false;
 	style::align _align = style::al_topleft;
diff --git a/ui/text/text_utilities.cpp b/ui/text/text_utilities.cpp
index 4d5035ea..70b6ebff 100644
--- a/ui/text/text_utilities.cpp
+++ b/ui/text/text_utilities.cpp
@@ -118,42 +118,47 @@ TextWithEntities Mid(const TextWithEntities &text, int position, int n) {
 		n = int(text.text.size()) - position;
 	}
 	const auto midEnd = (position + n);
-	auto entities = ranges::views::all(
-		text.entities
-	) | ranges::views::filter([&](const EntityInText &entity) {
+	// XP walk: range-v3 0.12 under v141_xp can't deduce ranges::to<QList<>>;
+	// materialize the filter+transform pipeline by hand.
+	auto entities = EntitiesInText();
+	for (const auto &entity : text.entities) {
 		// Intersects of ranges.
 		const auto l1 = entity.offset();
 		const auto r1 = entity.offset() + entity.length() - 1;
 		const auto l2 = position;
 		const auto r2 = midEnd - 1;
-		return !(l1 > r2 || l2 > r1);
-	}) | ranges::views::transform([&](const EntityInText &entity) {
+		if (l1 > r2 || l2 > r1) {
+			continue;
+		}
 		if ((entity.offset() == position) && (entity.length() == n)) {
-			return entity;
+			entities.push_back(entity);
+			continue;
 		}
 		const auto start = std::max(entity.offset(), position);
 		const auto end = std::min(entity.offset() + entity.length(), midEnd);
-		return EntityInText(
+		entities.push_back(EntityInText(
 			entity.type(),
 			start - position,
 			end - start,
-			entity.data());
-	}) | ranges::to<EntitiesInText>();
+			entity.data()));
+	}
 	return {
-		.text = text.text.mid(position, n),
-		.entities = std::move(entities),
+		text.text.mid(position, n),
+		std::move(entities),
 	};
 }
 
 TextWithEntities Filtered(
 		const TextWithEntities &text,
 		const std::vector<EntityType> &types) {
-	auto result = ranges::views::all(
-		text.entities
-	) | ranges::views::filter([&](const EntityInText &entity) {
-		return ranges::contains(types, entity.type());
-	}) | ranges::to<EntitiesInText>();
-	return { .text = text.text, .entities = std::move(result) };
+	// XP walk: manual materialization, ranges::to<QList<>> won't deduce here.
+	auto result = EntitiesInText();
+	for (const auto &entity : text.entities) {
+		if (ranges::contains(types, entity.type())) {
+			result.push_back(entity);
+		}
+	}
+	return { text.text, std::move(result) };
 }
 
 } // namespace Text
diff --git a/ui/toast/toast.cpp b/ui/toast/toast.cpp
index 9884a604..5aa9bd23 100644
--- a/ui/toast/toast.cpp
+++ b/ui/toast/toast.cpp
@@ -58,11 +58,11 @@ base::weak_ptr<Instance> Show(const Config &config) {
 base::weak_ptr<Instance> Show(
 		not_null<QWidget*> parent,
 		const QString &text) {
-	return Show(parent, Config{ .text = { text }, .st = &st::defaultToast });
+	return Show(parent, Config{ {}, { text }, &st::defaultToast }); // title, text, st
 }
 
 base::weak_ptr<Instance> Show(const QString &text) {
-	return Show(Config{ .text = { text }, .st = &st::defaultToast });
+	return Show(Config{ {}, { text }, &st::defaultToast }); // title, text, st
 }
 
 void Instance::shownAnimationCallback() {
diff --git a/ui/toast/toast_widget.cpp b/ui/toast/toast_widget.cpp
index 0907e1bd..f25db2ac 100644
--- a/ui/toast/toast_widget.cpp
+++ b/ui/toast/toast_widget.cpp
@@ -175,11 +175,29 @@ void Widget::paintEvent(QPaintEvent *e) {
 
 	p.setPen(st::toastFg);
 	_text.draw(p, {
-		.position = { _st->padding.left(), _textTop },
-		.availableWidth = _textWidth + 1,
-		.palette = &_st->palette,
-		.spoiler = Ui::Text::DefaultSpoilerCache(),
-		.elisionHeight = _maxTextHeight,
+		// XP walk: designated init -> positional (C7555). PaintContext layout:
+		// position, outerWidth, availableWidth, geometry, align, clip, palette,
+		// pre, blockquote, colors, spoiler, now, paused, pausedEmoji,
+		// pausedSpoiler, selection, fullWidthSelection(=true), elisionHeight, ...
+		{ _st->padding.left(), _textTop }, // position
+		{}, // outerWidth
+		_textWidth + 1, // availableWidth
+		{}, // geometry
+		style::al_left, // align (default; {} would be 0 != al_left)
+		{}, // clip
+		&_st->palette, // palette
+		{}, // pre
+		{}, // blockquote
+		{}, // colors
+		Ui::Text::DefaultSpoilerCache(), // spoiler
+		{}, // now
+		{}, // paused
+		{}, // pausedEmoji
+		{}, // pausedSpoiler
+		{}, // selection
+		true, // fullWidthSelection (default)
+		{}, // highlight -- XP walk: PaintContext highlight field(18) inserted (C++17 gap)
+		_maxTextHeight, // elisionHeight
 	});
 }
 
diff --git a/ui/ui_pch.h b/ui/ui_pch.h
index 1894747b..9c7ceaee 100644
--- a/ui/ui_pch.h
+++ b/ui/ui_pch.h
@@ -17,6 +17,7 @@
 #include <QtGui/QFont>
 #include <QtGui/QFontMetrics>
 #include <QtGui/QPainter>
+#include <QtGui/QPainterPath> // Qt 5.15 no longer pulls this in via <QPainter>.
 #include <QtGui/QImage>
 #include <QtGui/QPixmap>
 
@@ -31,3 +32,13 @@
 #include "base/basic_types.h"
 #include "base/flat_map.h"
 #include "base/flat_set.h"
+
+// Qt pulls <windows.h> (without WIN32_LEAN_AND_MEAN) which drags in rpcndr.h's
+// `#define small char` / `#define hyper __int64`, breaking ordinary identifiers
+// (generated style-struct members named `small`, locals in labels.cpp, ...).
+#ifdef small
+#undef small
+#endif
+#ifdef hyper
+#undef hyper
+#endif
diff --git a/ui/ui_utility.h b/ui/ui_utility.h
index 56019429..d640499d 100644
--- a/ui/ui_utility.h
+++ b/ui/ui_utility.h
@@ -14,6 +14,9 @@
 #include <QtCore/QEvent>
 #include <QtWidgets/QWidget>
 
+#include <type_traits>
+#include <utility>
+
 class QPixmap;
 class QImage;
 class QWheelEvent;
@@ -45,6 +48,24 @@ private:
 
 };
 
+// XP walk: C++20 requires-expressions in CreateChild replaced with C++17
+// void_t detectors for `.get()` / `.data()` member presence.
+template <typename T, typename = void>
+struct has_get_method : std::false_type {
+};
+template <typename T>
+struct has_get_method<T, std::void_t<decltype(std::declval<const T&>().get())>>
+	: std::true_type {
+};
+
+template <typename T, typename = void>
+struct has_data_method : std::false_type {
+};
+template <typename T>
+struct has_data_method<T, std::void_t<decltype(std::declval<const T&>().data())>>
+	: std::true_type {
+};
+
 } // namespace details
 
 template <typename Widget, typename ...Args>
@@ -68,10 +89,11 @@ inline Value *CreateChild(
 				parent,
 				std::forward<Args>(args)...)->value();
 		}
-	} else if constexpr (requires(const Parent & t) { t.get(); }) {
+	} else if constexpr (details::has_get_method<Parent>::value) {
+		// XP walk: C++20 requires-expr -> C++17 void_t detector (has_get_method).
 		return new Value(parent.get(), std::forward<Args>(args)...);
 	} else {
-		static_assert(requires(const Parent &t) { t.data(); });
+		static_assert(details::has_data_method<Parent>::value);
 		return new Value(parent.data(), std::forward<Args>(args)...);
 	}
 }
diff --git a/ui/widgets/elastic_scroll.cpp b/ui/widgets/elastic_scroll.cpp
index df757817..1cdf43cc 100644
--- a/ui/widgets/elastic_scroll.cpp
+++ b/ui/widgets/elastic_scroll.cpp
@@ -456,8 +456,8 @@ auto ElasticScroll::computeAccumulatedParts() const ->AccumulatedParts {
 	const auto relativeAccumulated = (1. - returnProgress)
 		* (_overscrollAccumulated - baseAccumulated);
 	return {
-		.base = baseAccumulated,
-		.relative = int(base::SafeRound(relativeAccumulated)),
+		baseAccumulated, // base
+		int(base::SafeRound(relativeAccumulated)), // relative
 	};
 }
 
@@ -927,9 +927,9 @@ void ElasticScroll::updateState() {
 		}
 	}
 	setState({
-		.visibleFrom = from,
-		.visibleTill = till,
-		.fullSize = nowFullSize,
+		from, // visibleFrom
+		till, // visibleTill
+		nowFullSize, // fullSize
 	});
 }
 
diff --git a/ui/widgets/elastic_scroll.h b/ui/widgets/elastic_scroll.h
index e9053414..1ef5cc64 100644
--- a/ui/widgets/elastic_scroll.h
+++ b/ui/widgets/elastic_scroll.h
@@ -29,12 +29,18 @@ struct ScrollState {
 	int visibleTill = 0;
 	int fullSize = 0;
 
-	friend inline constexpr auto operator<=>(
-		const ScrollState &,
-		const ScrollState &) = default;
-	friend inline constexpr bool operator==(
-		const ScrollState &,
-		const ScrollState &) = default;
+	friend inline bool operator==(
+			const ScrollState &a,
+			const ScrollState &b) {
+		return (a.visibleFrom == b.visibleFrom)
+			&& (a.visibleTill == b.visibleTill)
+			&& (a.fullSize == b.fullSize);
+	}
+	friend inline bool operator!=(
+			const ScrollState &a,
+			const ScrollState &b) {
+		return !(a == b);
+	}
 };
 
 class ElasticScrollBar final : public RpWidget {
@@ -80,11 +86,11 @@ private:
 	QRect _bar;
 	QPoint _dragPosition;
 	ScrollState _state;
-	bool _shown : 1 = false;
-	bool _over : 1 = false;
-	bool _overBar : 1 = false;
-	bool _vertical : 1 = false;
-	bool _dragging : 1 = false;
+	bool _shown = false;
+	bool _over = false;
+	bool _overBar = false;
+	bool _vertical = false;
+	bool _dragging = false;
 
 };
 
@@ -92,12 +98,16 @@ struct ElasticScrollPosition {
 	int value = 0;
 	int overscroll = 0;
 
-	friend inline auto operator<=>(
-		ElasticScrollPosition,
-		ElasticScrollPosition) = default;
 	friend inline bool operator==(
-		ElasticScrollPosition,
-		ElasticScrollPosition) = default;
+			ElasticScrollPosition a,
+			ElasticScrollPosition b) {
+		return (a.value == b.value) && (a.overscroll == b.overscroll);
+	}
+	friend inline bool operator!=(
+			ElasticScrollPosition a,
+			ElasticScrollPosition b) {
+		return !(a == b);
+	}
 };
 
 enum class ElasticScrollMovement {
@@ -243,17 +253,17 @@ private:
 	TouchScrollState _touchScrollState = TouchScrollState::Manual;
 	int _overscrollAccumulated = 0;
 	int _ignoreMomentumFromOverscroll = 0;
-	bool _touchDisabled : 1 = false;
-	bool _touchScroll : 1 = false;
-	bool _touchPress : 1 = false;
-	bool _touchRightButton : 1 = false;
-	bool _touchPreviousPositionValid : 1 = false;
-	bool _touchWaitingAcceleration : 1 = false;
-	bool _vertical : 1 = false;
-	bool _widgetAcceptsTouch : 1 = false;
-	bool _disabled : 1 = false;
-	bool _dirtyState : 1 = false;
-	bool _overscrollReturning : 1 = false;
+	bool _touchDisabled = false;
+	bool _touchScroll = false;
+	bool _touchPress = false;
+	bool _touchRightButton = false;
+	bool _touchPreviousPositionValid = false;
+	bool _touchWaitingAcceleration = false;
+	bool _vertical = false;
+	bool _widgetAcceptsTouch = false;
+	bool _disabled = false;
+	bool _dirtyState = false;
+	bool _overscrollReturning = false;
 
 	Fn<bool(not_null<QWheelEvent*>)> _customWheelProcess;
 	Fn<bool(not_null<QTouchEvent*>)> _customTouchProcess;
diff --git a/ui/widgets/fields/custom_field_object.cpp b/ui/widgets/fields/custom_field_object.cpp
index 677fe1f1..d2b71311 100644
--- a/ui/widgets/fields/custom_field_object.cpp
+++ b/ui/widgets/fields/custom_field_object.cpp
@@ -156,19 +156,18 @@ void CustomFieldObject::drawObject(
 		const auto top = 0;
 		const auto id = format.property(InputField::kQuoteId).toInt();
 		if (const auto i = _quotes.find(id); i != end(_quotes)) {
-			i->second.string.draw(*painter, {
-				.position = QPoint(left + rect.x(), top + rect.y()),
-				.outerWidth = int(base::SafeRound(doc->pageSize().width())),
-				.availableWidth = int(std::floor(rect.width())),
-
-				.palette = nullptr,
-				.spoiler = Text::DefaultSpoilerCache(),
-				.now = _now,
-				.pausedEmoji = _pausedEmoji(),
-				.pausedSpoiler = _pausedSpoiler(),
-
-				.elisionLines = Text::kQuoteCollapsedLines,
-			});
+			// XP walk: designated -> named-local (C7555; PaintContext many fields).
+			auto context = Ui::Text::PaintContext();
+			context.position = QPoint(left + rect.x(), top + rect.y());
+			context.outerWidth = int(base::SafeRound(doc->pageSize().width()));
+			context.availableWidth = int(std::floor(rect.width()));
+			context.palette = nullptr;
+			context.spoiler = Text::DefaultSpoilerCache();
+			context.now = _now;
+			context.pausedEmoji = _pausedEmoji();
+			context.pausedSpoiler = _pausedSpoiler();
+			context.elisionLines = Text::kQuoteCollapsedLines;
+			i->second.string.draw(*painter, context);
 		}
 		return;
 	}
@@ -188,12 +187,16 @@ void CustomFieldObject::drawObject(
 		return;
 	}
 	i->second->paint(*painter, {
-		.textColor = format.foreground().color(),
-		.now = _now,
-		.position = QPoint(
+		// XP walk: designated -> positional (C7555). CustomEmojiPaintContext:
+		// textColor, size, now, scale, position, paused.
+		format.foreground().color(), // textColor
+		{}, // size
+		_now, // now
+		{}, // scale
+		QPoint(
 			int(base::SafeRound(rect.x())) + st::emojiPadding + _skip,
-			int(base::SafeRound(rect.y())) + _skip),
-		.paused = _pausedEmoji && _pausedEmoji(),
+			int(base::SafeRound(rect.y())) + _skip), // position
+		_pausedEmoji && _pausedEmoji(), // paused
 	});
 }
 
diff --git a/ui/widgets/fields/input_field.cpp b/ui/widgets/fields/input_field.cpp
index a834b834..62cdcc23 100644
--- a/ui/widgets/fields/input_field.cpp
+++ b/ui/widgets/fields/input_field.cpp
@@ -2099,8 +2099,11 @@ void InputField::paintQuotes(QPaintEvent *e) {
 					&& (rect.height() > collapsedCutoff);
 				Text::ValidateQuotePaintCache(*cache, *st);
 				Text::FillQuotePaint(*p, target, *cache, *st, {
-					.expandIcon = collapsed,
-					.collapseIcon = collapsible,
+					// XP walk: designated -> positional (C7555). SkipBlockPaintParts.
+					{}, // skippedTop
+					{}, // skipBottom
+					collapsed, // expandIcon
+					collapsible, // collapseIcon
 				});
 				if (!pre) {
 					_blockquoteBg = cache->bg;
diff --git a/ui/widgets/fields/input_field.h b/ui/widgets/fields/input_field.h
index cdc76439..b058c4f9 100644
--- a/ui/widgets/fields/input_field.h
+++ b/ui/widgets/fields/input_field.h
@@ -14,6 +14,11 @@
 #include "ui/text/text_entity.h"
 #include "ui/text/text_custom_emoji.h"
 
+#include <QtGui/QTextObjectInterface>
+#include <QtCore/QTimer>
+#include <QtGui/QPainterPath> // QPainterPath member below; Qt 5.15 needs it explicit and the moc TU has no PCH.
+#include <QShortcut>
+
 #include <rpl/variable.h>
 
 #include <QtGui/QTextCursor>
@@ -80,14 +85,30 @@ class CustomFieldObject;
 struct MarkdownEnabled {
 	base::flat_set<QString> tagsSubset;
 
+	// XP walk: C++20 defaulted operator== -> manual ==/!= (C7589).
 	friend inline bool operator==(
-		const MarkdownEnabled &,
-		const MarkdownEnabled &) = default;
+			const MarkdownEnabled &a,
+			const MarkdownEnabled &b) {
+		return a.tagsSubset == b.tagsSubset;
+	}
+	friend inline bool operator!=(
+			const MarkdownEnabled &a,
+			const MarkdownEnabled &b) {
+		return !(a == b);
+	}
 };
 struct MarkdownDisabled {
+	// XP walk: C++20 defaulted operator== -> manual ==/!= (C7589).
 	friend inline bool operator==(
-		const MarkdownDisabled &,
-		const MarkdownDisabled &) = default;
+			const MarkdownDisabled &,
+			const MarkdownDisabled &) {
+		return true;
+	}
+	friend inline bool operator!=(
+			const MarkdownDisabled &,
+			const MarkdownDisabled &) {
+		return false;
+	}
 };
 struct MarkdownEnabledState {
 	std::variant<MarkdownDisabled, MarkdownEnabled> data;
@@ -95,17 +116,33 @@ struct MarkdownEnabledState {
 	[[nodiscard]] bool disabled() const;
 	[[nodiscard]] bool enabledForTag(QStringView tag) const;
 
+	// XP walk: C++20 defaulted operator== -> manual ==/!= (C7589).
 	friend inline bool operator==(
-		const MarkdownEnabledState &,
-		const MarkdownEnabledState &) = default;
+			const MarkdownEnabledState &a,
+			const MarkdownEnabledState &b) {
+		return a.data == b.data;
+	}
+	friend inline bool operator!=(
+			const MarkdownEnabledState &a,
+			const MarkdownEnabledState &b) {
+		return !(a == b);
+	}
 };
 struct InputFieldTextRange {
 	int from = 0;
 	int till = 0;
 
+	// XP walk: C++20 defaulted operator== -> manual ==/!= (C7589).
 	friend inline bool operator==(
-		InputFieldTextRange,
-		InputFieldTextRange) = default;
+			InputFieldTextRange a,
+			InputFieldTextRange b) {
+		return (a.from == b.from) && (a.till == b.till);
+	}
+	friend inline bool operator!=(
+			InputFieldTextRange a,
+			InputFieldTextRange b) {
+		return !(a == b);
+	}
 
 	[[nodiscard]] bool empty() const {
 		return (till <= from);
diff --git a/ui/widgets/labels.cpp b/ui/widgets/labels.cpp
index a1aaeb27..38e446ec 100644
--- a/ui/widgets/labels.cpp
+++ b/ui/widgets/labels.cpp
@@ -685,11 +685,11 @@ void FlatLabel::showContextMenu(QContextMenuEvent *e, ContextMenuReason reason)
 
 	_contextMenu = base::make_unique_q<PopupMenu>(this, _stMenu);
 	const auto request = ContextMenuRequest{
-		.menu = _contextMenu.get(),
-		.link = ClickHandler::getActive(),
-		.selection = _selectable ? _selection : TextSelection(),
-		.uponSelection = uponSelection,
-		.fullSelection = _selectable && _text.isFullSelection(_selection),
+		_contextMenu.get(), // menu
+		ClickHandler::getActive(), // link
+		_selectable ? _selection : TextSelection(), // selection (v5.0.0: replaced hasSelection bool) // XP walk: designated -> positional (C7555)
+		uponSelection, // uponSelection
+		_selectable && _text.isFullSelection(_selection), // fullSelection
 	};
 
 	if (_contextMenuHook) {
@@ -796,7 +796,7 @@ CrossFadeAnimation::Data FlatLabel::crossFadeData(
 	result.full = GrabWidgetToImage(this, QRect(), bg->c);
 	const auto textWidth = width() - _st.margin.left() - _st.margin.right();
 	result.lineWidths = _text.countLineWidths(textWidth, {
-		.breakEverywhere = _breakEverywhere,
+		_breakEverywhere, // breakEverywhere -- XP walk: designated -> positional (C7555)
 	});
 	result.lineHeight = _st.style.font->height;
 	const auto addedHeight = (_st.style.lineHeight - result.lineHeight);
@@ -972,20 +972,35 @@ void FlatLabel::paintEvent(QPaintEvent *e) {
 		? _animationsPausedCallback()
 		: WhichAnimationsPaused::None;
 	_text.draw(p, {
-		.position = { textLeft, _st.margin.top() },
-		.availableWidth = textWidth,
-		.align = _st.align,
-		.clip = e->rect(),
-		.palette = &_st.palette,
-		.spoiler = Text::DefaultSpoilerCache(),
-		.now = crl::now(),
-		.pausedEmoji = (paused == WhichAnimationsPaused::CustomEmoji
-			|| paused == WhichAnimationsPaused::All),
-		.pausedSpoiler = (paused == WhichAnimationsPaused::Spoiler
-			|| paused == WhichAnimationsPaused::All),
-		.selection = selection,
-		.elisionHeight = elisionHeight,
-		.elisionBreakEverywhere = renderElided && _breakEverywhere,
+		// XP walk: designated init -> positional (C7555). New PaintContext order:
+		// position, outerWidth, availableWidth, geometry, align, clip, palette,
+		// pre, blockquote, colors, spoiler, now, paused, pausedEmoji,
+		// pausedSpoiler, selection, fullWidthSelection(=true), highlight, elisionHeight,
+		// elisionLines(v4.11.4 NEW), elisionRemoveFromEnd, elisionBreakEverywhere.
+		{ textLeft, _st.margin.top() }, // position
+		{}, // outerWidth
+		textWidth, // availableWidth
+		{}, // geometry
+		_st.align, // align
+		e->rect(), // clip
+		&_st.palette, // palette
+		{}, // pre
+		{}, // blockquote
+		{}, // colors
+		Text::DefaultSpoilerCache(), // spoiler
+		crl::now(), // now
+		{}, // paused
+		(paused == WhichAnimationsPaused::CustomEmoji
+			|| paused == WhichAnimationsPaused::All), // pausedEmoji
+		(paused == WhichAnimationsPaused::Spoiler
+			|| paused == WhichAnimationsPaused::All), // pausedSpoiler
+		selection, // selection
+		true, // fullWidthSelection (default)
+		{}, // highlight -- XP walk: PaintContext highlight field(18) inserted (C++17 gap)
+		elisionHeight, // elisionHeight
+		{}, // elisionLines -- XP walk: v4.11.4 NEW field(20); 0 -> renderer derives from elisionHeight
+		{}, // elisionRemoveFromEnd
+		renderElided && _breakEverywhere, // elisionBreakEverywhere
 	});
 }
 
diff --git a/ui/widgets/popup_menu.cpp b/ui/widgets/popup_menu.cpp
index c7a89d6b..cfbc5637 100644
--- a/ui/widgets/popup_menu.cpp
+++ b/ui/widgets/popup_menu.cpp
@@ -21,7 +21,7 @@
 #include <QtGui/QtEvents>
 #include <QtGui/QPainter>
 #include <QtGui/QScreen>
-#include <QtGui/QWindow>
+#include <QtGui/QWindow> // XP walk: Qt 5.15.16-XP lacks QWidget::setScreen.
 #include <QtWidgets/QApplication>
 #include <private/qapplication_p.h>
 
@@ -458,18 +458,18 @@ void PopupMenu::paintEvent(QPaintEvent *e) {
 			? _showAnimation->paintFrame(p, 0, 0, width(), progress, opacity)
 			: PanelAnimation::PaintState();
 		_showStateChanges.fire({
-			.opacity = state.opacity,
-			.widthProgress = state.widthProgress,
-			.heightProgress = state.heightProgress,
-			.appearingWidth = state.width,
-			.appearingHeight = state.height,
-			.appearing = true,
+			state.opacity,
+			state.widthProgress,
+			state.heightProgress,
+			state.width,
+			state.height,
+			true,
 		});
 	} else if (_a_opacity.animating()) {
 		if (_showAnimation) {
 			_showAnimation.reset();
 			_showStateChanges.fire({
-				.toggling = true,
+				1., 1., 1., {}, {}, {}, true,
 			});
 		}
 		p.setOpacity(_a_opacity.value(0.));
@@ -846,12 +846,12 @@ void PopupMenu::fireCurrentShowState() {
 		_a_show.value(1.),
 		_a_opacity.value(1.));
 	_showStateChanges.fire({
-		.opacity = state.opacity,
-		.widthProgress = state.widthProgress,
-		.heightProgress = state.heightProgress,
-		.appearingWidth = state.width,
-		.appearingHeight = state.height,
-		.appearing = true,
+		state.opacity,
+		state.widthProgress,
+		state.heightProgress,
+		state.width,
+		state.height,
+		true,
 	});
 }
 
@@ -966,17 +966,15 @@ bool PopupMenu::prepareGeometryFor(const QPoint &p, PopupMenu *parent) {
 	windowHandle()->removeEventFilter(this);
 	windowHandle()->installEventFilter(this);
 	if (_parent) {
-#if QT_VERSION >= QT_VERSION_CHECK(6, 0, 0)
-		setScreen(_parent->screen());
-#else // Qt >= 6.0.0
-		windowHandle()->setScreen(_parent->screen());
-#endif // Qt < 6.0.0
+		// XP walk: QWidget::setScreen is absent in Qt 5.15.16-XP; move the
+		// underlying window handle instead (no-op until the popup is created).
+		if (const auto handle = windowHandle()) {
+			handle->setScreen(_parent->screen());
+		}
 	} else if (screen) {
-#if QT_VERSION >= QT_VERSION_CHECK(6, 0, 0)
-		setScreen(screen);
-#else // Qt >= 6.0.0
-		windowHandle()->setScreen(screen);
-#endif // Qt < 6.0.0
+		if (const auto handle = windowHandle()) {
+			handle->setScreen(screen);
+		}
 	}
 	validateCompositingSupport();
 
diff --git a/ui/widgets/scroll_area.cpp b/ui/widgets/scroll_area.cpp
index c8acf08c..99655e87 100644
--- a/ui/widgets/scroll_area.cpp
+++ b/ui/widgets/scroll_area.cpp
@@ -77,14 +77,14 @@ void ScrollBar::updateBar(bool force) {
 			bool newTopSh = (_st->topsh < 0), newBottomSh = (_st->bottomsh < 0);
 			if (newTopSh != _topSh || force) {
 				_shadowVisibilityChanged.fire({
-					.type = ScrollShadow::Type::Top,
-					.visible = (_topSh = newTopSh),
+					ScrollShadow::Type::Top,
+					(_topSh = newTopSh),
 				});
 			}
 			if (newBottomSh != _bottomSh || force) {
 				_shadowVisibilityChanged.fire({
-					.type = ScrollShadow::Type::Bottom,
-					.visible = (_bottomSh = newBottomSh),
+					ScrollShadow::Type::Bottom,
+					(_bottomSh = newBottomSh),
 				});
 			}
 			return;
@@ -116,14 +116,14 @@ void ScrollBar::updateBar(bool force) {
 		bool newTopSh = (_st->topsh < 0) || (area()->scrollTop() > _st->topsh), newBottomSh = (_st->bottomsh < 0) || (area()->scrollTop() < area()->scrollTopMax() - _st->bottomsh);
 		if (newTopSh != _topSh || force) {
 			_shadowVisibilityChanged.fire({
-				.type = ScrollShadow::Type::Top,
-				.visible = (_topSh = newTopSh),
+				ScrollShadow::Type::Top,
+				(_topSh = newTopSh),
 			});
 		}
 		if (newBottomSh != _bottomSh || force) {
 			_shadowVisibilityChanged.fire({
-				.type = ScrollShadow::Type::Bottom,
-				.visible = (_bottomSh = newBottomSh),
+				ScrollShadow::Type::Bottom,
+				(_bottomSh = newBottomSh),
 			});
 		}
 	}
diff --git a/ui/widgets/shadow.cpp b/ui/widgets/shadow.cpp
index efbedf9f..ccd1c149 100644
--- a/ui/widgets/shadow.cpp
+++ b/ui/widgets/shadow.cpp
@@ -150,15 +150,15 @@ void Shadow::paint(
 		const std::array<QImage, 4> &corners,
 		RectParts sides) {
 	const auto shadow = CustomShadowCorners{
-		.left = st.left,
-		.topLeft = CustomImage(corners[0]),
-		.top = st.top,
-		.topRight = CustomImage(corners[2]),
-		.right = st.right,
-		.bottomRight = CustomImage(corners[3]),
-		.bottom = st.bottom,
-		.bottomLeft = CustomImage(corners[1]),
-		.extend = st.extend,
+		st.left,
+		CustomImage(corners[0]),
+		st.top,
+		CustomImage(corners[2]),
+		st.right,
+		CustomImage(corners[3]),
+		st.bottom,
+		CustomImage(corners[1]),
+		st.extend,
 	};
 	ShadowPaint<CustomShadowCorners>(p, box, outerWidth, shadow, sides);
 }
@@ -171,15 +171,15 @@ void Shadow::paint(
 		const std::array<QImage, 4> &sides,
 		const std::array<QImage, 4> &corners) {
 	const auto shadow = CustomShadow{
-		.left = CustomImage(sides[0]),
-		.topLeft = CustomImage(corners[0]),
-		.top = CustomImage(sides[1]),
-		.topRight = CustomImage(corners[2]),
-		.right = CustomImage(sides[2]),
-		.bottomRight = CustomImage(corners[3]),
-		.bottom = CustomImage(sides[3]),
-		.bottomLeft = CustomImage(corners[1]),
-		.extend = st.extend,
+		CustomImage(sides[0]),
+		CustomImage(corners[0]),
+		CustomImage(sides[1]),
+		CustomImage(corners[2]),
+		CustomImage(sides[2]),
+		CustomImage(corners[3]),
+		CustomImage(sides[3]),
+		CustomImage(corners[1]),
+		st.extend,
 	};
 	ShadowPaint<CustomShadow>(p, box, outerWidth, shadow, RectPart()
 		| (sides[0].isNull() ? RectPart() : RectPart::Left)
diff --git a/ui/widgets/side_bar_button.cpp b/ui/widgets/side_bar_button.cpp
index 470d94af..053fdf40 100644
--- a/ui/widgets/side_bar_button.cpp
+++ b/ui/widgets/side_bar_button.cpp
@@ -168,7 +168,8 @@ void SideBarButton::paintEvent(QPaintEvent *e) {
 	if (_lock.locked) {
 		const auto lineWidths = _text.countLineWidths(
 			width() - 2 * _st.textSkip,
-			{ .reserve = kMaxLabelLines });
+			// XP walk: designated -> positional (C7555). LineWidthsOptions{breakEverywhere, reserve}.
+			{ false, kMaxLabelLines });
 		if (lineWidths.empty()) {
 			return;
 		}
diff --git a/ui/widgets/tooltip.cpp b/ui/widgets/tooltip.cpp
index 350a5b71..4ce93d43 100644
--- a/ui/widgets/tooltip.cpp
+++ b/ui/widgets/tooltip.cpp
@@ -14,7 +14,7 @@
 #include "styles/style_widgets.h"
 
 #include <QtGui/QScreen>
-#include <QtGui/QWindow>
+#include <QtGui/QWindow> // XP walk: Qt 5.15.16-XP lacks QWidget::setScreen.
 #include <QtWidgets/QApplication>
 
 namespace Ui {
