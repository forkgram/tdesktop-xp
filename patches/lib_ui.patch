diff --git a/ui/gl/gl_detection.cpp b/ui/gl/gl_detection.cpp
index 7ee57676..6313eff6 100644
--- a/ui/gl/gl_detection.cpp
+++ b/ui/gl/gl_detection.cpp
@@ -111,7 +111,7 @@ Capabilities CheckCapabilities(QWidget *widget) {
 	const auto version = u"%1.%2"_q
 		.arg(supported.majorVersion())
 		.arg(supported.majorVersion());
-	auto result = Capabilities{ .supported = true };
+	auto result = Capabilities{ true };
 	if (supported.alphaBufferSize() >= 8) {
 		result.transparency = true;
 		LOG_ONCE(("OpenGL: QOpenGLContext created, version: %1."
diff --git a/ui/gl/gl_image.cpp b/ui/gl/gl_image.cpp
index 56bb89f8..662a377d 100644
--- a/ui/gl/gl_image.cpp
+++ b/ui/gl/gl_image.cpp
@@ -130,8 +130,8 @@ TexturedRect Image::texturedRect(
 		: clip.intersected(geometry);
 	if (visible.isEmpty()) {
 		return TexturedRect{
-			.geometry = Rect(visible),
-			.texture = Rect(0., 0., 0., 0.),
+			Rect(visible),
+			Rect(0., 0., 0., 0.),
 		};
 	}
 	const auto xFactor = texture.width() / geometry.width();
@@ -146,8 +146,8 @@ TexturedRect Image::texturedRect(
 		? _subimage
 		: _textureSize);
 	return {
-		.geometry = Rect(visible),
-		.texture = Rect(QRectF(
+		Rect(visible),
+		Rect(QRectF(
 			usedTexture.x() / dimensions.width(),
 			usedTexture.y() / dimensions.height(),
 			usedTexture.width() / dimensions.width(),
diff --git a/ui/gl/gl_shader.cpp b/ui/gl/gl_shader.cpp
index bf51d057..399094d1 100644
--- a/ui/gl/gl_shader.cpp
+++ b/ui/gl/gl_shader.cpp
@@ -56,21 +56,21 @@ void main() {
 ShaderPart VertexPassTextureCoord(char prefix) {
 	const auto name = prefix + QString("_texcoord");
 	return {
-		.header = R"(
+		R"(
 attribute vec2 )" + name + R"(In;
 varying vec2 )" + name + ";\n",
-		.body = R"(
+		R"(
 	)" + name + " = " + name + "In;\n",
 	};
 }
 
 ShaderPart FragmentSampleARGB32Texture() {
 	return {
-		.header = R"(
+		R"(
 varying vec2 v_texcoord;
 uniform sampler2D s_texture;
 )",
-		.body = R"(
+		R"(
 	result = texture2D(s_texture, v_texcoord);
 	result = vec4(result.b, result.g, result.r, result.a);
 )",
@@ -79,13 +79,13 @@ uniform sampler2D s_texture;
 
 ShaderPart FragmentSampleYUV420Texture() {
 	return {
-		.header = R"(
+		R"(
 varying vec2 v_texcoord;
 uniform sampler2D y_texture;
 uniform sampler2D u_texture;
 uniform sampler2D v_texture;
 )",
-		.body = R"(
+		R"(
 	float y = texture2D(y_texture, v_texcoord).r - 0.0625;
 	float u = texture2D(u_texture, v_texcoord).r - 0.5;
 	float v = texture2D(v_texture, v_texcoord).r - 0.5;
@@ -100,10 +100,10 @@ uniform sampler2D v_texture;
 
 ShaderPart FragmentGlobalOpacity() {
 	return {
-		.header = R"(
+		R"(
 uniform float g_opacity;
 )",
-		.body = R"(
+		R"(
 	result *= g_opacity;
 )",
 	};
@@ -111,7 +111,7 @@ uniform float g_opacity;
 
 ShaderPart VertexViewportTransform() {
 	return {
-		.header = R"(
+		R"(
 uniform vec2 viewport;
 vec4 transform(vec4 position) {
 	return vec4(
@@ -120,7 +120,7 @@ vec4 transform(vec4 position) {
 		position.w);
 }
 )",
-		.body = R"(
+		R"(
 	result = transform(result);
 )",
 	};
@@ -128,7 +128,7 @@ vec4 transform(vec4 position) {
 
 ShaderPart FragmentRoundCorners() {
 	return {
-		.header = R"(
+		R"(
 uniform vec4 roundRect;
 uniform vec2 radiusOutline;
 uniform vec4 roundBg;
@@ -149,7 +149,7 @@ vec2 roundedCorner() {
 		1. - (smoothstep(0., 1., outline) * outlineFg.a));
 }
 )",
-		.body = R"(
+		R"(
 	vec2 roundOutline = roundedCorner();
 	result = result * roundOutline.y
 		+ vec4(outlineFg.rgb, 1) * (1. - roundOutline.y);
@@ -160,10 +160,10 @@ vec2 roundedCorner() {
 
 ShaderPart FragmentStaticColor() {
 	return {
-		.header = R"(
+		R"(
 uniform vec4 s_color;
 )",
-		.body = R"(
+		R"(
 	result = s_color;
 )",
 	};
diff --git a/ui/paint/arcs.cpp b/ui/paint/arcs.cpp
index d3121d6c..2341f796 100644
--- a/ui/paint/arcs.cpp
+++ b/ui/paint/arcs.cpp
@@ -56,9 +56,10 @@ void ArcsAnimation::initArcs(std::vector<float> thresholds) {
 		const auto threshold = thresholds[i];
 		const auto progress = (threshold > _currentValue) ? 1. : 0.;
 		auto arc = Arc{
-			.rect = computeArcRect(i + 1),
-			.threshold = threshold,
-			.progress = progress,
+			computeArcRect(i + 1),
+			threshold,
+			0, // startTime (skipped by upstream designated init)
+			progress,
 		};
 		_arcs.push_back(std::move(arc));
 	}
diff --git a/ui/platform/win/ui_utility_win.cpp b/ui/platform/win/ui_utility_win.cpp
index 034299f2..5869bf5a 100644
--- a/ui/platform/win/ui_utility_win.cpp
+++ b/ui/platform/win/ui_utility_win.cpp
@@ -56,22 +56,10 @@ std::optional<bool> IsOverlapped(
 	const auto handle = reinterpret_cast<HWND>(widget->window()->winId());
 	Expects(handle != nullptr);
 
-	ComPtr<IVirtualDesktopManager> virtualDesktopManager;
-	HRESULT hr = CoCreateInstance(
-		CLSID_VirtualDesktopManager,
-		nullptr,
-		CLSCTX_ALL,
-		IID_PPV_ARGS(&virtualDesktopManager));
-	
-	if (SUCCEEDED(hr)) {
-		BOOL isCurrent;
-		hr = virtualDesktopManager->IsWindowOnCurrentVirtualDesktop(
-			handle,
-			&isCurrent);
-		if (SUCCEEDED(hr) && !isCurrent) {
-			return true;
-		}
-	}
+	// XP walk: IVirtualDesktopManager / CLSID_VirtualDesktopManager are a Win10
+	// shell API absent from the XP SDK (7.1A). Windows XP has no virtual desktops,
+	// so skip the off-current-desktop check and fall through to the plain
+	// z-order window-intersection test below.
 
 	std::vector<HWND> visited;
 	const RECT nativeRect{
@@ -113,7 +101,8 @@ bool ShowWindowMenu(QWindow *window) {
 
 TitleControls::Layout TitleControlsLayout() {
 	return TitleControls::Layout{
-		.right = {
+		{}, // left (skipped by upstream designated init)
+		{
 			TitleControls::Control::Minimize,
 			TitleControls::Control::Maximize,
 			TitleControls::Control::Close,
diff --git a/ui/style/style_core.h b/ui/style/style_core.h
index cd9324f7..fb6c4d95 100644
--- a/ui/style/style_core.h
+++ b/ui/style/style_core.h
@@ -75,3 +75,14 @@ inline QImage createInvertedCircleMask(int size) {
 }
 
 } // namespace style
+
+// <windows.h> (via Qt, no WIN32_LEAN_AND_MEAN) drags in rpcndr.h's
+// `#define small char` / `#define hyper __int64`. Generated style structs have
+// members named `small`; generated style headers include this one right before
+// those structs, so undefining here keeps the identifiers usable.
+#ifdef small
+#undef small
+#endif
+#ifdef hyper
+#undef hyper
+#endif
diff --git a/ui/text/text.cpp b/ui/text/text.cpp
index 2f07c5c0..7d15f078 100644
--- a/ui/text/text.cpp
+++ b/ui/text/text.cpp
@@ -737,8 +737,9 @@ bool Parser::readCommand() {
 		createBlock();
 		int32 len = _ptr->unicode();
 		_links.push_back(EntityLinkData{
-			.data = QString(++_ptr, len),
-			.type = EntityType::CustomUrl
+			{},
+			QString(++_ptr, len),
+			EntityType::CustomUrl
 		});
 		_lnkIndex = kStringLinkIndexShift + _links.size();
 	} break;
diff --git a/ui/toast/toast.cpp b/ui/toast/toast.cpp
index b2016ba8..fc0a8636 100644
--- a/ui/toast/toast.cpp
+++ b/ui/toast/toast.cpp
@@ -57,11 +57,11 @@ base::weak_ptr<Instance> Show(const Config &config) {
 base::weak_ptr<Instance> Show(
 		not_null<QWidget*> parent,
 		const QString &text) {
-	return Show(parent, Config{ .text = { text }, .st = &st::defaultToast });
+	return Show(parent, Config{ { text }, &st::defaultToast });
 }
 
 base::weak_ptr<Instance> Show(const QString &text) {
-	return Show(Config{ .text = { text }, .st = &st::defaultToast });
+	return Show(Config{ { text }, &st::defaultToast });
 }
 
 void Instance::shownAnimationCallback() {
diff --git a/ui/ui_pch.h b/ui/ui_pch.h
index 1894747b..9c7ceaee 100644
--- a/ui/ui_pch.h
+++ b/ui/ui_pch.h
@@ -17,6 +17,7 @@
 #include <QtGui/QFont>
 #include <QtGui/QFontMetrics>
 #include <QtGui/QPainter>
+#include <QtGui/QPainterPath> // Qt 5.15 no longer pulls this in via <QPainter>.
 #include <QtGui/QImage>
 #include <QtGui/QPixmap>
 
@@ -31,3 +32,13 @@
 #include "base/basic_types.h"
 #include "base/flat_map.h"
 #include "base/flat_set.h"
+
+// Qt pulls <windows.h> (without WIN32_LEAN_AND_MEAN) which drags in rpcndr.h's
+// `#define small char` / `#define hyper __int64`, breaking ordinary identifiers
+// (generated style-struct members named `small`, locals in labels.cpp, ...).
+#ifdef small
+#undef small
+#endif
+#ifdef hyper
+#undef hyper
+#endif
diff --git a/ui/widgets/input_fields.h b/ui/widgets/input_fields.h
index 42646518..7d751a08 100644
--- a/ui/widgets/input_fields.h
+++ b/ui/widgets/input_fields.h
@@ -16,6 +16,7 @@
 #include <QtWidgets/QLineEdit>
 #include <QtWidgets/QTextEdit>
 #include <QtCore/QTimer>
+#include <QtGui/QPainterPath> // QPainterPath member below; Qt 5.15 needs it explicit and the moc TU has no PCH.
 
 class QTouchEvent;
 class Painter;
diff --git a/ui/widgets/labels.cpp b/ui/widgets/labels.cpp
index d64d8593..654270f3 100644
--- a/ui/widgets/labels.cpp
+++ b/ui/widgets/labels.cpp
@@ -309,14 +309,16 @@ int FlatLabel::countTextWidth() const {
 		&& _allowedWidth < _text.maxWidth()
 		&& _tryMakeSimilarLines) {
 		auto large = _allowedWidth;
-		auto small = _allowedWidth / 2;
+		// `small` is a macro (`#define small char`) from rpcndr.h pulled in via
+		// <windows.h>; use a different name here.
+		auto smallWidth = _allowedWidth / 2;
 		const auto largeHeight = _text.countHeight(large, _breakEverywhere);
-		while (large - small > 1) {
-			const auto middle = (large + small) / 2;
+		while (large - smallWidth > 1) {
+			const auto middle = (large + smallWidth) / 2;
 			if (largeHeight == _text.countHeight(middle, _breakEverywhere)) {
 				large = middle;
 			} else {
-				small = middle;
+				smallWidth = middle;
 			}
 		}
 		return large;
